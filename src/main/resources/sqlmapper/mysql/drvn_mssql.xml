<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.mindone.ems.drvn.DrvnMapper">
	<select id="getPumpCombTime" parameterType="String" resultType="String">
		SELECT
			DATE_SUB(MAX(RGSTR_TIME), INTERVAL 3 MINUTE)
		from
			TB_CTR_PUMPYN_RST
		WHERE
			RGSTR_TIME &lt;= DATE_ADD(#{nowDate}, INTERVAL 10 MINUTE)
<!--		AND-->
<!--			RGSTR_TIME &gt; DATE_SUB(#{nowDate}, INTERVAL 1 DAY);-->
	</select>
	<select id="getPumpCombLogTime" parameterType="String" resultType="String">
		SELECT
		DATE_FORMAT(MAX(RGSTR_TIME), '%Y-%m-%d %H:%i')
		from
		TB_CTR_PUMPYN_RST
	</select>
	<!-- 실측 펌프(PMB_TAG) 기준 TB_RAWDATA 최신 TS. 예측 주기 비종속. pump_grp=0이면 전 그룹. -->
	<select id="selectLatestPumpRawTs" parameterType="hashmap" resultType="String">
		<!-- TS 하한(최근 1일) 추가: 없으면 TB_RAWDATA 전체 이력을 풀스캔해 MAX(TS)를 구하느라
		     데이터 증가에 따라 점점 느려져 20초+(커넥션 leak 경고)까지 갔음. 최신 pump raw TS 는
		     항상 최근에 있으므로 1일 윈도로 제한해 TS 인덱스 레인지를 타게 한다.
		     (1일 내 수집이 전무하면 MAX=null → 호출부가 startDate 로 폴백) -->
		SELECT DATE_FORMAT(MAX(tr.TS), '%Y-%m-%d %H:%i:%s')
		FROM TB_RAWDATA tr
		JOIN TB_CTR_PRF_PUMPMST_INF tcppi ON tr.TAGNAME = tcppi.PMB_TAG
		WHERE tr.TS &gt;= DATE_SUB(NOW(), INTERVAL 1 DAY)
		<if test="pump_grp != 0">
			AND tcppi.PUMP_GRP = #{pump_grp}
		</if>
	</select>
	<select id="selectPumpPressure" resultType="hashmap" parameterType="hashmap">
		select DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts, tr.VALUE as value
		from TB_RAWDATA tr, TB_CTR_PRF_PUMPMST_INF tcppi
		WHERE
		tr.TAGNAME = tcppi.PRI_T_TAG
			AND
			tcppi.PUMP_GRP = #{pump_grp}
			<if test="first == true">
				and tr.TS &lt;= #{startDate}
				and tr.TS &gt; DATE_SUB(#{startDate}, INTERVAL #{cycle} minute)
				order by tr.TS desc limit 1;
			</if>
			<if test="first != true">
				and tr.TS &lt; #{startDate}
				and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
				and MINUTE(tr.TS) % #{cycle} = 0
				group by tr.TS
				order by tr.TS;
			</if>
	</select>
	<select id="tunnelWaterLevel" resultType="hashmap" parameterType="hashmap">
		select DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts, tr.VALUE as value
		from TB_RAWDATA tr, TB_CTR_PRF_PUMPMST_INF tcppi
		WHERE
		tr.TAGNAME = tcppi.LEI_TAG
		<if test="first == true">
			and tr.TS &lt;= #{startDate}
			and tr.TS &gt; DATE_SUB(#{startDate}, INTERVAL #{cycle} minute)
			order by tr.TS desc limit 1;
		</if>
		<if test="first != true">
			and tr.TS &lt; #{startDate}
			and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
			and MINUTE(tr.TS) % #{cycle} = 0
			group by tr.TS
			order by tr.TS;
		</if>
	</select>
	<select id="selectPumpFlow" resultType="hashmap" parameterType="hashmap">
		select DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts, tr.VALUE as value
		from TB_RAWDATA tr, TB_CTR_PRF_PUMPMST_INF tcppi
		WHERE
		tr.TAGNAME = tcppi.FRI_TAG
			AND
			tcppi.PUMP_GRP = #{pump_grp}
			<if test="first == true">
				and tr.TS &lt;= #{startDate}
				and tr.TS &gt; DATE_SUB(#{startDate}, INTERVAL #{cycle} minute)
				order by tr.TS desc limit 1;
			</if>
			<if test="first != true">
				and tr.TS &lt; #{startDate}
				and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
				and MINUTE(tr.TS) % #{cycle} = 0
				group by tr.TS
				order by tr.TS;
			</if>
	</select>
	
	<select id="selectNowPumpUse" parameterType="hashmap" resultType="hashmap">
		SELECT
		tr_spi.TS,
		tcppi.PUMP_TYP as PUMP_TYP,
		tr_spi.TAGNAME AS spi_tagname,
		tr_pmb.TAGNAME AS pmb_tagname,
		tr_spi.VALUE AS pump_freq,
		tr_pmb.VALUE AS pump_use,
		tcppi.PUMP_IDX,
		tcppi.PUMP_GRP_IDX,
		tcppi.PUMP_GRP_NM,
		tcppi.PUMP_NM
		FROM
		TB_CTR_PRF_PUMPMST_INF tcppi
		LEFT JOIN
		TB_RAWDATA tr_spi ON tr_spi.TAGNAME = tcppi.SPI_TAG AND tr_spi.TS = (select max(TS) from TB_RAWDATA where TAGNAME = tcppi.SPI_TAG and TS &lt;= #{actl_ref_ts} and TS &gt; DATE_SUB(#{actl_ref_ts}, interval 10 minute))
		LEFT JOIN
		TB_RAWDATA tr_pmb ON tr_pmb.TAGNAME = tcppi.PMB_TAG AND tr_pmb.TS = (select max(TS) from TB_RAWDATA where TAGNAME = tcppi.PMB_TAG and TS &lt;= #{actl_ref_ts} and TS &gt; DATE_SUB(#{actl_ref_ts}, interval 10 minute))
		<if test="pump_grp != 0">
			WHERE
			tcppi.PUMP_GRP = #{pump_grp}
		</if>
		ORDER BY
		tcppi.PUMP_IDX;
	</select>
	<select id="getPumpUse" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(tr_pmb.TS, '%Y-%m-%d %H:%i') as TS,
			tcppi.PUMP_TYP AS PUMP_TYP,
			tr_pmb.VALUE AS pump_use,
			tcppi.PUMP_GRP_IDX as PUMP_IDX,
			tcppi.PUMP_GRP,
			tcppi.PUMP_GRP_DSC as PUMP_GRP_NM,
			tcppi.PUMP_NM
		FROM
			TB_CTR_PRF_PUMPMST_INF tcppi
			LEFT JOIN
				TB_RAWDATA tr_pmb ON tr_pmb.TAGNAME = tcppi.PMB_TAG
		WHERE
			<if test="first == true">
					tr_pmb.TS = (SELECT MAX(TS) FROM TB_RAWDATA WHERE TS &lt;= #{startDate} AND TS &gt; DATE_SUB(#{startDate}, INTERVAL 10 MINUTE))
			</if>
			<if test="first != true">
					tr_pmb.TS &lt;= DATE_SUB(#{startDate}, INTERVAL #{cycle} MINUTE)
				AND
					tr_pmb.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} HOUR)
				AND
					MINUTE(tr_pmb.TS) % #{cycle} = 0
				GROUP BY
					tr_pmb.TS, tcppi.PUMP_GRP, tcppi.PUMP_IDX
			</if>
		ORDER BY
			tr_pmb.TS, tcppi.PUMP_GRP, tcppi.PUMP_IDX;
	</select>
	<select id="getPumpFreqUse" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(tr_spi.TS, '%Y-%m-%d %H:%i') as TS,
			tcppi.PUMP_TYP AS PUMP_TYP,
			CAST(tr_spi.VALUE AS VARCHAR(10)) AS pump_freq,
			tcppi.PUMP_GRP_IDX as PUMP_IDX,
			tcppi.PUMP_GRP,
			tcppi.PUMP_GRP_DSC as PUMP_GRP_NM,
			tcppi.PUMP_NM
		FROM
			TB_CTR_PRF_PUMPMST_INF tcppi
			LEFT JOIN
				TB_RAWDATA tr_spi ON tr_spi.TAGNAME = tcppi.SPI_TAG
		WHERE
			<if test="first == true">
					tr_spi.TS = (SELECT MAX(TS) FROM TB_RAWDATA WHERE TS &lt;= #{startDate} AND TS &gt; DATE_SUB(#{startDate}, INTERVAL 10 MINUTE))
			</if>
			<if test="first != true">
					tr_spi.TS &lt;= DATE_SUB(#{startDate}, INTERVAL #{cycle} MINUTE)
				AND
					tr_spi.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} HOUR)
				AND
					MINUTE(tr_spi.TS) % #{cycle} = 0
				GROUP BY
					tr_spi.TS, tcppi.PUMP_GRP, tcppi.PUMP_IDX
			</if>
		ORDER BY
			tr_spi.TS, tcppi.PUMP_GRP, tcppi.PUMP_IDX;
	</select>
	<select id="selectNowPumpPwrUse" parameterType="hashmap" resultType="hashmap">
		SELECT
		tr.TS,
		tcppi.PUMP_IDX,
		tcppi.PUMP_GRP_IDX,
		tr.TAGNAME,
		tr.VALUE * tpti.PWI_UNIT_VALUE as value
		FROM
		TB_RAWDATA tr,
		TB_CTR_PRF_PUMPMST_INF tcppi,
		TB_PEAK_TAG_INF tpti
		WHERE
		tr.TAGNAME = tcppi.PWI_TAG
		AND
		tr.TAGNAME  = tpti.PWI_TAG
		<if test="pump_grp != 0">
			AND
			tcppi.PUMP_GRP = #{pump_grp}
		</if>
		AND
		tr.TS = (select max(TS) from TB_RAWDATA where TAGNAME = tcppi.PWI_TAG and TS &lt;= #{actl_ref_ts} and TS &gt; DATE_SUB(#{actl_ref_ts}, interval 10 minute))
		<!--tr.TS = #{startDate};-->
		ORDER BY tcppi.PUMP_IDX ASC
	</select>
	<select id="selectHeadLoss" resultType="double" parameterType="hashmap">
		SELECT
			sum(tlv.HH_LOSS_VAL) as lossVal
		from TB_FR_VAL tlv
			join TB_LINK_GRP tlg on tlg.LINK_ID = tlv.LINK_ID
			JOIN TB_TOT_GRP ttg ON tlg.GRP_NM = ttg.GRP_KEY
		where
				UPPER(tlv.FLG) = UPPER(#{opt_idx})
			AND
				ttg.TOT_GRP = #{GRP_NM}
			and
				tlv.RGSTR_TIME = (
				select max(rgstr_time) from TB_FR_VAL where rgstr_time &lt;= #{startDate} and rgstr_time &gt;= DATE_SUB(#{startDate}, interval 3 minute)
			)
		GROUP BY
			tlv.RGSTR_TIME
		order by
		tlv.RGSTR_TIME desc limit 1;
	</select>
	
	<select id="selectForHeadLoss" resultType="hashmap" parameterType="hashmap">
		SELECT
			SUM(tlv.HH_LOSS_VAL) AS lossVal,
			tlv.RGSTR_TIME as ts
		FROM
			TB_FR_VAL tlv
			JOIN TB_LINK_GRP tlg ON tlg.LINK_ID = tlv.LINK_ID
			JOIN TB_TOT_GRP ttg ON tlg.GRP_NM = ttg.GRP_KEY
		WHERE
			UPPER(tlv.FLG) = UPPER(#{opt_idx})
		AND
			ttg.TOT_GRP = #{GRP_NM}
		AND
			tlv.RGSTR_TIME &lt;= #{startDate}
		and
			tlv.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		and
			MINUTE(tlv.RGSTR_TIME) % #{cycle} = 0
		GROUP BY
			tlv.RGSTR_TIME
		ORDER BY
			tlv.RGSTR_TIME
	</select>
	
	<select id="selectGsAllLinkRange" resultType="hashmap" parameterType="hashmap">
		SELECT
			FLW_ALG_RST_VAL AS linkVal,
			RGSTR_TIME as ts
		FROM
		TB_FR_VAL
		WHERE
			UPPER(FLG) = UPPER(#{opt_idx})
		AND
		LINK_ID = #{link_id}
		AND
		RGSTR_TIME &lt;= #{startDate}
		and
		RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		and
		MINUTE(RGSTR_TIME) % #{cycle} = 0
		GROUP BY
		RGSTR_TIME
		ORDER BY
		RGSTR_TIME
	</select>
	
	<select id="selectGsAllNodeRange" resultType="hashmap" parameterType="hashmap">
		SELECT
		FP_ALG_RST_VAL AS nodeVal,
		RGSTR_TIME as ts
		FROM
		TB_FP_VAL
		WHERE
		UPPER(FLG) = UPPER(#{opt_idx})
		AND
		NODE_ID = #{node_id}
		AND
		RGSTR_TIME &lt;= #{startDate}
		and
		RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		and
		MINUTE(RGSTR_TIME) % #{cycle} = 0
		GROUP BY
		RGSTR_TIME
		ORDER BY
		RGSTR_TIME
	</select>
	
	<select id="selectGsAllCurLinkRange" resultType="hashmap" parameterType="hashmap">
		SELECT
		FLW_ALG_RST_VAL AS linkVal,
		RGSTR_TIME as ts
		FROM
		TB_FR_SI_VAL
		WHERE
		LINK_ID = #{link_id}
		AND
		RGSTR_TIME &lt;= #{startDate}
		and
		RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		and
		MINUTE(RGSTR_TIME) % #{cycle} = 0
		and
		FLG = 'mo'
		GROUP BY
		RGSTR_TIME
		ORDER BY
		RGSTR_TIME
	</select>
	
	<select id="selectGsAllCurNodeRange" resultType="hashmap" parameterType="hashmap">
		SELECT
		FP_ALG_RST_VAL AS nodeVal,
		RGSTR_TIME as ts
		FROM
		TB_FP_SI_VAL
		WHERE
		NODE_ID = #{node_id}
		AND
		RGSTR_TIME &lt;= #{startDate}
		and
		RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		and
		MINUTE(RGSTR_TIME) % #{cycle} = 0
		and
		FLG = 'mo'
		GROUP BY
		RGSTR_TIME
		ORDER BY
		RGSTR_TIME
	</select>
	
	<select id="selectGsAllNodeFirst" resultType="double" parameterType="hashmap">
		SELECT
		FP_ALG_RST_VAL AS nodeVal
		FROM
		TB_FP_VAL
		WHERE
		UPPER(FLG) = UPPER(#{opt_idx})
		AND
		NODE_ID = #{node_id}
		AND
		RGSTR_TIME = (
		select max(rgstr_time) from TB_FP_VAL where rgstr_time &lt;= #{startDate} and rgstr_time &gt;= DATE_SUB(#{startDate}, interval 3 minute) and UPPER(FLG) = UPPER(#{opt_idx})
		)
		GROUP BY
		RGSTR_TIME
		order by
		RGSTR_TIME desc limit 1;
	</select>
	
	<select id="selectGsAllLinkFirst" resultType="double" parameterType="hashmap">
		SELECT
		FLW_ALG_RST_VAL AS linkVal
		FROM
		TB_FR_VAL
		WHERE
		UPPER(FLG) = UPPER(#{opt_idx})
		AND
		LINK_ID = #{link_id}
		AND
		RGSTR_TIME = (
		select max(rgstr_time) from TB_FR_VAL where rgstr_time &lt;= #{startDate} and rgstr_time &gt;= DATE_SUB(#{startDate}, interval 3 minute) and UPPER(FLG) = UPPER(#{opt_idx})
		)
		GROUP BY
		RGSTR_TIME
		order by
		RGSTR_TIME desc limit 1;
	</select>
	<select id="selectGsAllCurNodeFirst" resultType="double" parameterType="hashmap">
		SELECT
		FP_ALG_RST_VAL AS nodeVal
		FROM
		TB_FP_SI_VAL
		WHERE
		FLG = 'mo'
		AND
		NODE_ID = #{node_id}
		AND
		RGSTR_TIME = (
		select max(rgstr_time) from TB_FP_SI_VAL where rgstr_time &lt;= #{startDate} and rgstr_time &gt;= DATE_SUB(#{startDate}, interval 3 minute) and FLG = 'mo'
		)
		GROUP BY
		RGSTR_TIME
		order by
		RGSTR_TIME desc limit 1;
	</select>
	
	<select id="selectGsAllCurLinkFirst" resultType="double" parameterType="hashmap">
		SELECT
		FLW_ALG_RST_VAL AS linkVal
		FROM
		TB_FR_SI_VAL
		WHERE
		FLG = 'mo'
		AND
		LINK_ID = #{link_id}
		AND
		RGSTR_TIME = (
		select max(rgstr_time) from TB_FR_SI_VAL where rgstr_time &lt;= #{startDate} and rgstr_time &gt;= DATE_SUB(#{startDate}, interval 3 minute) and FLG = 'mo'
		)
		GROUP BY
		RGSTR_TIME
		order by
		RGSTR_TIME desc limit 1;
	</select>
	
	<select id="selectPrdctFlowPressure" parameterType="hashmap" resultType="hashmap">
		SELECT
			tctr.DSTRB_ID as junction_id,
			tctr.PRDCT_VALUE as value,
			DATE_FORMAT(tctr.PRDCT_TIME, '%Y-%m-%d %H:%i') as ts
		from
			TB_CTR_TNK_RST tctr
		WHERE
			UPPER(tctr.DSTRB_ID) = UPPER(#{DSTRB_ID})
			and tctr.MODEL_NAME = 'XGBoost'
			and
			tctr.PRDCT_TIME = DATE_ADD(tctr.RGSTR_TIME, INTERVAL 10 minute)
			and
		<if test="first == true">
			PRDCT_TIME &gt;= #{startDate}
			order by
				tctr.PRDCT_TIME asc limit 1
		</if>
		<if test="first != true">
					<if test="intradotion == true">
						tctr.PRDCT_TIME &lt;= #{startDate}
					</if>
					<if test="intradotion != true">
						tctr.PRDCT_TIME &lt;= #{startDate}
					</if>
					and
						tctr.PRDCT_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
					and
						MINUTE(tctr.PRDCT_TIME) % #{cycle} = 0
				group by tctr.PRDCT_TIME
			order by
				tctr.PRDCT_TIME
		</if>
		
	</select>
	
	<select id="prdctFlowPressure" parameterType="hashmap" resultType="hashmap">
		<!--
		  10분 단위 예측: 가장 최근 RGSTR_TIME(수행 시점) 의 10분 후(PRDCT_TIME) 예측 행 1건만 반환.
		  ts 는 PRDCT_TIME 기준 (예측 시점) 으로 표기.
		-->
		SELECT
			tctr.PRDCT_VALUE as value,
			DATE_FORMAT(tctr.PRDCT_TIME, '%Y-%m-%d %H:%i') as ts
		from
			TB_CTR_TNK_RST tctr
		WHERE
			UPPER(tctr.DSTRB_ID) = UPPER(#{DSTRB_ID})
			and tctr.MODEL_NAME = 'XGBoost'
			and tctr.RGSTR_TIME = (
				SELECT MAX(RGSTR_TIME) FROM TB_CTR_TNK_RST
				 WHERE UPPER(DSTRB_ID) = UPPER(#{DSTRB_ID})
				   AND RGSTR_TIME &lt;= #{nowDateTime}
				   AND MODEL_NAME = 'XGBoost'
			)
			and tctr.PRDCT_TIME = DATE_ADD(tctr.RGSTR_TIME, INTERVAL 10 MINUTE)
		limit 1
	</select>

	<select id="countPredictionReady" parameterType="hashmap" resultType="int">
		<!--
		  이번 10분 예측 배치 도착 판정.
		  대상 DSTRB_ID 중 RGSTR_TIME 이 [batchStart, batchEnd) 범위에 적재된 distinct DSTRB_ID 개수.
		  호출부에서 이 개수가 대상 DSTRB_ID 수와 같으면 '도착 완료'로 간주.
		-->
		SELECT COUNT(DISTINCT UPPER(DSTRB_ID))
		FROM TB_CTR_TNK_RST
		WHERE UPPER(DSTRB_ID) IN
		<foreach item="id" collection="dstrbIds" open="(" separator="," close=")">
			UPPER(#{id})
		</foreach>
			AND RGSTR_TIME &gt;= #{batchStart}
			AND RGSTR_TIME &lt; #{batchEnd}
			AND MODEL_NAME = 'XGBoost'
	</select>

	<select id="selectTargetLevelByTags" resultType="hashMap">
		<!-- 태그·시간별 목표수위(TB_TARGET_LEVEL). 매일 업서트되는 테이블이므로
		     (TAG,HOURS) 별 최신 MDFCN_DT(마지막 갱신) 행만 반환한다.
		     CRT_DT 는 최초 생성시각이라 갱신 판단 기준으로 부적합 → MDFCN_DT 사용. -->
		SELECT t.TAG, t.HOURS, t.MIN_VL, t.MAX_VL, t.USER_MIN_VL, t.USER_MAX_VL
		FROM TB_TARGET_LEVEL t
		JOIN (
			SELECT TAG, HOURS, MAX(MDFCN_DT) AS MX_MDFCN
			FROM TB_TARGET_LEVEL
			WHERE UPPER(TAG) IN
			<foreach item="tag" collection="tags" open="(" separator="," close=")">
				UPPER(#{tag})
			</foreach>
			GROUP BY TAG, HOURS
		) m ON m.TAG = t.TAG AND m.HOURS = t.HOURS AND m.MX_MDFCN = t.MDFCN_DT
	</select>

	<!--
	  임의 horizon(분) 예측 조회. prdctFlowPressure 의 일반화 버전.
	  horizonMin = 10/60/120/180/360 등.
	-->
	<select id="prdctFlowPressureByHorizon" parameterType="hashmap" resultType="hashmap">
		SELECT
			tctr.PRDCT_VALUE as value,
			DATE_FORMAT(tctr.PRDCT_TIME, '%Y-%m-%d %H:%i') as ts
		from
			TB_CTR_TNK_RST tctr
		WHERE
			UPPER(tctr.DSTRB_ID) = UPPER(#{DSTRB_ID})
			and tctr.MODEL_NAME = 'XGBoost'
			and tctr.RGSTR_TIME = (
				SELECT MAX(RGSTR_TIME) FROM TB_CTR_TNK_RST
				 WHERE UPPER(DSTRB_ID) = UPPER(#{DSTRB_ID})
				   AND RGSTR_TIME &lt;= #{nowDateTime}
				   AND MODEL_NAME = 'XGBoost'
			)
			and tctr.PRDCT_TIME = DATE_ADD(tctr.RGSTR_TIME, INTERVAL #{horizonMin} MINUTE)
		limit 1
	</select>
	<select id="curFlowPressure" parameterType="hashmap" resultType="hashmap">
		SELECT
		VALUE as value,
		DATE_FORMAT(TS, '%Y-%m-%d %H:%i') as ts
		from
		TB_RAWDATA
		WHERE
		TAGNAME = #{DSTRB_ID}
		and
		TS &lt;= DATE_SUB(#{nowDateTime}, interval 1 minute)
		and
		TS &gt;= DATE_SUB(#{nowDateTime}, interval 5 minute)
		group by
		TS
		order by
		TS desc limit 1
	</select>
	<select id="preFlowPressure" parameterType="hashmap" resultType="hashmap">
		SELECT
			CAST(PRDCT_VALUE as CHAR) as value,
			DATE_FORMAT(RGSTR_TIME, '%Y-%m-%d %H:%i') as ts
		FROM
			TB_CTR_TNK_RST
		WHERE
				UPPER(DSTRB_ID) = UPPER(#{DSTRB_ID})
			AND
				MODEL_NAME = 'XGBoost'
			AND
				RGSTR_TIME &lt;= #{nowDateTime}
			AND
				RGSTR_TIME &gt;= DATE_SUB(#{nowDateTime}, interval 10 minute)
		GROUP BY
			RGSTR_TIME
		ORDER BY
			RGSTR_TIME
		DESC LIMIT 1;
	</select>
	
	
	<select id="selectPreIntradotion" parameterType="hashmap" resultType="hashmap">
		SELECT
			tctr.PRDCT_VALUE as value,
			DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
		<if test="type == 'flow'">
			tnt.FRI_TAG as tag,
			tnt.DSTRB_Q_ID as dstrb_id
			from
				TB_CTR_TNK_INF tnt
			join
				TB_CTR_TNK_RST tctr on tnt.DSTRB_Q_ID = tctr.DSTRB_ID
			WHERE
				UPPER(tnt.DSTRB_Q_ID) in
					<foreach item="id" collection="dstrb_id" open="(" separator="," close=")">
						UPPER(#{id})
					</foreach>
		</if>
		<if test="type == 'pressure'">
			tnt.PRI_TAG as tag,
			tnt.DSTRB_P_ID as dstrb_id
			from
				TB_CTR_TNK_INF tnt
			join
				TB_CTR_TNK_RST tctr on tnt.DSTRB_P_ID = tctr.DSTRB_ID
			WHERE
				UPPER(tnt.DSTRB_P_ID) in
					<foreach item="id" collection="dstrb_id" open="(" separator="," close=")">
						UPPER(#{id})
					</foreach>
		</if>
		and
		<if test="first == true">
			tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
			and
			tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, interval 3 minute)
			order by tctr.RGSTR_TIME
			limit 1;
		</if>
		<if test="first != true">
			tctr.RGSTR_TIME &lt;= #{startDate}
			and
			tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
			order by tctr.RGSTR_TIME;
		</if>
	</select>
	<sql id="setDate12HourAgo">
		WITH RECURSIVE A AS
		(
			SELECT STR_TO_DATE(DATE_SUB(#{startDate}, INTERVAL 12 hour), '%Y-%m-%d %H:%i:00') AS TS
			UNION ALL
			SELECT DATE_ADD(A.TS, INTERVAL 1 MINUTE) AS TS FROM A
			WHERE A.TS &lt;= STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:00')
		)
	</sql>
	<sql id="setDate1MinuteAfter">
		WITH RECURSIVE A AS
		(
			SELECT STR_TO_DATE(DATE_ADD(#{startDate}, INTERVAL 1 MINUTE), '%Y-%m-%d %H:%i:00') AS TS
		)
	</sql>
	
	<select id="selectIntradotion" parameterType="hashmap" resultType="hashmap">
		SELECT
			tr.TS,
			tr.VALUE as value,
			tnt.PRI_TAG as tag,
			tnt.DSTRB_P_ID as dstrb_id
		from
		    TB_RAWDATA tr
			LEFT JOIN
				TB_CTR_TNK_INF tnt ON tnt.PRI_TAG = tr.TAGNAME
		WHERE
			tnt.DSTRB_P_ID in
				<foreach item="id" collection="dstrb_id" open="(" separator="," close=")">
					#{id}
				</foreach>
			AND
				tr.TS &lt;= #{startDate}
			AND
				tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
			AND
				MINUTE(tr.TS) % 5 = 0
		order by tr.TS;
	</select>
	<select id="grPreesureList" parameterType="hashmap" resultType="hashmap">
		SELECT
			tr.TS,
			tr.VALUE as value,
			tr.TAGNAME as tag
		from
			TB_RAWDATA tr
		WHERE
			tr.TAGNAME IN (
			'780-344-PRC-6004',
			'780-344-PRC-4004'
		)
		AND
		tr.TS &lt;= #{startDate}
		AND
		tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
		AND
		MINUTE(tr.TS) % 5 = 0
		order by tr.TS;
	</select>
	<select id="selectCurFlowData" resultType="hashmap" parameterType="hashmap">
		SELECT
			tr.TS,
			tr.TAGNAME as tag,
			tr.VALUE as value
		FROM
			TB_RAWDATA tr
		WHERE
			tr.TAGNAME in
				<foreach item="tag" collection="tagList" open="(" separator="," close=")">
					#{tag}
				</foreach>
			AND
				tr.TS &lt;= #{startDate}
			AND
				tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
			AND
				MINUTE(tr.TS) % 5 = 0
		order by tr.TS;
	</select>
	<select id="select12HourCurPumpFlow" resultType="hashmap" parameterType="hashmap">
		SELECT tr.TS, tr.VALUE as value
		FROM TB_RAWDATA tr
		WHERE tr.TAGNAME IN (
		SELECT FRI_TAG
		FROM TB_CTR_PRF_PUMPMST_INF
		WHERE PUMP_GRP = #{pump_grp}
		)
		AND tr.TS BETWEEN DATE_SUB(#{startDate}, INTERVAL 12 HOUR) AND #{startDate}
		AND MINUTE(tr.TS) % 5 = 0
		GROUP BY tr.TS
		ORDER BY tr.TS;
	</select>
	<select id="select12HourCurPumpPressure" resultType="hashmap" parameterType="hashmap">
		SELECT
		tr.TS,
		tr.VALUE AS value
		FROM
		TB_RAWDATA tr
		WHERE
		tr.TAGNAME IN (
		SELECT PRI_T_TAG
		FROM TB_CTR_PRF_PUMPMST_INF
		WHERE PUMP_GRP = #{pump_grp}
		)
		AND tr.TS BETWEEN DATE_SUB(#{startDate}, INTERVAL 12 HOUR) AND #{startDate}
		AND MINUTE(tr.TS) % 5 = 0
		GROUP BY
		tr.TS
		ORDER BY
		tr.TS
	</select>
	
	<select id="selectPreFlow" resultType="hashmap" parameterType="hashmap">
		SELECT
			tctr.RGSTR_TIME as ts,
			tctr.PRDCT_VALUE as value,
			tnt.FRI_TAG as tag,
			tnt.DSTRB_Q_ID as dstrb_id
		from
			TB_CTR_TNK_RST tctr
			LEFT JOIN
				TB_CTR_TNK_INF tnt on tnt.DSTRB_Q_ID = tctr.DSTRB_ID
		WHERE
			tctr.MODEL_NAME = 'XGBoost'
			AND UPPER(tnt.DSTRB_Q_ID) in
			<foreach item="id" collection="dstrb_id" open="(" separator="," close=")">
				UPPER(#{id})
			</foreach>
			AND
			<if test="first != true">
					tctr.RGSTR_TIME &lt;= #{startDate}
				AND
					tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
				order by
					tctr.RGSTR_TIME
			</if>
			<if test="first == true">
					tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, INTERVAL 3 MINUTE)
				AND
					tctr.RGSTR_TIME &gt;= #{startDate}
				order by
					tctr.RGSTR_TIME desc
				limit
					1
			</if>

	</select>
	<select id="selectPrePri" resultType="hashmap" parameterType="hashmap">
		SELECT
			tctr.RGSTR_TIME as ts,
			tctr.PRDCT_VALUE as value,
			tnt.PRI_TAG as tag,
			tnt.DSTRB_P_ID as dstrb_id
		from
			TB_CTR_TNK_RST tctr
			LEFT JOIN
				TB_CTR_TNK_INF tnt on tnt.DSTRB_P_ID = tctr.DSTRB_ID
		WHERE
			tctr.MODEL_NAME = 'XGBoost'
			AND UPPER(tnt.DSTRB_P_ID) in
					<foreach item="id" collection="dstrb_id" open="(" separator="," close=")">
						UPPER(#{id})
					</foreach>
			AND
			<if test="first != true">
					tctr.RGSTR_TIME &lt;= #{startDate}
				AND
					tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
				order by
					tctr.RGSTR_TIME
			</if>
			<if test="first == true">
					tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, INTERVAL 3 MINUTE)
				AND
					tctr.RGSTR_TIME &gt;= #{startDate}
				order by
					tctr.RGSTR_TIME desc
				limit
					1
			</if>
	</select>
	<select id="grPrePressure" resultType="hashmap" parameterType="hashmap">
		SELECT
			COALESCE(inquiry.RGSTR_TIME, rst.RGSTR_TIME) AS ts,
			ROUND(CAST(COALESCE(inquiry.TUBE_PRSR_PRDCT, rst.TUBE_PRSR_PRDCT) AS FLOAT), 1) AS value,
			'780-344-PRC-6004' as tag
		FROM
				TB_CTR_PUMPYN_RST AS rst
			LEFT JOIN
				TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_GRP = inquiry.PUMP_GRP AND rst.RGSTR_TIME = inquiry.RGSTR_TIME
		WHERE
				rst.PUMP_GRP = 2
			AND
		<if test="first != true">
				rst.RGSTR_TIME &lt;= #{startDate}
			AND
				rst.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
			AND
				MINUTE(rst.RGSTR_TIME) % 5 = 0
			GROUP BY
				rst.RGSTR_TIME
			ORDER BY
				rst.RGSTR_TIME
		</if>
		<if test="first == true">
				rst.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, INTERVAL 3 MINUTE)
			AND
				rst.RGSTR_TIME &gt;= #{startDate}
			GROUP BY
				rst.RGSTR_TIME
			ORDER BY
				rst.RGSTR_TIME DESC
			limit
				1
		</if>
	</select>
	<select id="selectPreWithDstrb" parameterType="hashmap" resultType="hashmap">
		SELECT
			tctr.RGSTR_TIME as ts,
			tctr.DSTRB_ID as junction_id,
			tctr.PRDCT_VALUE as value
		from
			TB_CTR_TNK_RST tctr
		WHERE
			UPPER(tctr.DSTRB_ID) = UPPER(#{DSTRB_ID})
			AND tctr.MODEL_NAME = 'XGBoost'
			AND
			<if test="first != true">
					tctr.RGSTR_TIME &lt;= #{startDate}
				AND
					tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 12 hour)
				order by
					tctr.RGSTR_TIME
			</if>
			<if test="first == true">
					tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, INTERVAL 3 MINUTE)
				AND
					tctr.RGSTR_TIME &gt;= #{startDate}
				order by
					tctr.RGSTR_TIME desc
				limit
					1
			</if>
	</select>
	<select id="selectPumpCombCalGosan" resultType="hashmap">
		SELECT
		tpc.PUMP_GRP,
		tpc.C_IDX,
		tpc.C_ORD,
		tpc.FC_VAL,
		tpc.P_ADD_VAL,
		tpc.CS_OP,
		tpc.P_MUL_VAL,
		tpc.SS_OP,
		tpc.P_SQRT_MUL_VAL,
		tpc.PUMP_COMB,
		tpc.PUMP_COUNT,
		tpc.PUMP_PRIORITY,
		tpc.COUNT_IDX,
		tpc.USE_YN
		FROM
		TB_PUMP_CAL tpc
		LEFT JOIN TB_PUMP_COMBINATION_INF tpci
		ON tpc.PUMP_COUNT = tpci.PUMP_COUNT
		AND tpc.PUMP_PRIORITY = tpci.PUMP_PRIORITY
		WHERE
		tpci.USE_YN = 1
		AND
		tpc.USE_YN = 1
		ORDER BY tpc.PUMP_GRP, tpc.C_IDX, tpc.C_ORD,  tpc.PUMP_COUNT , tpc.PUMP_PRIORITY;
	</select>
	
	<select id="selectAverageValueLast10Minutes" resultType="double">
		<!--
		  EMPTY 보정용 fallback: 10분 차이(RGSTR_TIME→PRDCT_TIME = +10분) 예측 행 중
		  최근 10건 PRDCT_VALUE 평균. 0/NULL 행은 제외.
		-->
		SELECT AVG(PRDCT_VALUE)
		FROM (
			SELECT PRDCT_VALUE
			FROM TB_CTR_TNK_RST
			WHERE DSTRB_ID = #{DSTRB_ID}
			  AND MODEL_NAME = 'XGBoost'
			  AND PRDCT_VALUE IS NOT NULL
			  AND PRDCT_VALUE != 0
			  AND RGSTR_TIME <![CDATA[ <= ]]> #{nowDateTime}
			  AND PRDCT_TIME = DATE_ADD(RGSTR_TIME, INTERVAL 10 MINUTE)
			ORDER BY RGSTR_TIME DESC
			LIMIT 10
		) AS recent_data
	</select>
	
	<select id="selectPumpCombCal" resultType="hashmap">
		SELECT
		cal.PUMP_GRP,
		cal.C_IDX,
		cal.C_ORD,
		cal.FC_VAL,
		cal.P_ADD_VAL,
		cal.CS_OP,
		cal.P_MUL_VAL,
		cal.SS_OP,
		cal.P_SQRT_MUL_VAL,
		-- C_ORD가 1인 경우에만 조합 펌프를 보여줍니다.
		CASE
		WHEN cal.C_ORD = 1 THEN IFNULL(combo.ACTIVE_PUMPS, cal.PUMP_COMB)
		ELSE cal.PUMP_COMB
		END AS PUMP_COMB,
		cal.PUMP_COUNT,
		cal.PUMP_PRIORITY,
		cal.COUNT_IDX,
		cal.USE_YN
		FROM TB_PUMP_CAL AS cal
		-- TB_PUMP_COMBINATION_INF 테이블을 INNER JOIN하여 USE_YN이 1인 조합만 선택
		INNER JOIN TB_PUMP_COMBINATION_INF AS inf
		ON cal.PUMP_GRP = inf.PUMP_GRP
		AND cal.PUMP_COUNT = inf.PUMP_COUNT
		AND cal.PUMP_PRIORITY = inf.PUMP_PRIORITY
		AND inf.USE_YN = 1
		LEFT JOIN (
		SELECT
		PUMP_GRP,
		PUMP_COUNT,
		PUMP_PRIORITY,
		GROUP_CONCAT(PUMP_IDX ORDER BY PUMP_IDX) AS ACTIVE_PUMPS
		FROM TB_PUMP_COMBINATION
		WHERE PUMP_YN = 1
		GROUP BY PUMP_GRP, PUMP_COUNT, PUMP_PRIORITY
		) AS combo
		ON cal.PUMP_GRP = combo.PUMP_GRP
		AND cal.PUMP_COUNT = combo.PUMP_COUNT
		AND cal.PUMP_PRIORITY = combo.PUMP_PRIORITY
		WHERE cal.USE_YN = 1
		ORDER BY
		cal.PUMP_GRP,
		cal.C_IDX,
		cal.C_ORD,
		cal.PUMP_COUNT,
		cal.PUMP_PRIORITY;
	</select>
	<select id="selectPreTnkFlowPressure" resultType="hashmap" parameterType="hashmap">
		SELECT * FROM (
		SELECT
			DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
			tctr.PRDCT_VALUE as value,
			tcti.SRTTN as srttn
		from
			TB_CTR_TNK_INF tcti
		join
			<if test="find == 'flow'">
				TB_CTR_TNK_RST tctr on UPPER(tcti.DSTRB_Q_ID) = UPPER(tctr.DSTRB_ID)
			</if>
			<if test="find == 'press'">
				TB_CTR_TNK_RST tctr on UPPER(tcti.DSTRB_P_ID) = UPPER(tctr.DSTRB_ID)
			</if>
		WHERE
			tcti.TYPE = #{type}
			AND tctr.MODEL_NAME = 'XGBoost'
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
			tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
		AND
			tctr.RGSTR_TIME &gt;= DATE_SUB(DATE_ADD(#{startDate}, interval 5 minute), INTERVAL #{range} hour)
		AND
			MINUTE(tctr.RGSTR_TIME) % #{cycle} = 0
		UNION ALL
		SELECT
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
		tctr.PRDCT_VALUE as value,
		tcti.SRTTN as srttn
		from
		TB_CTR_TNK_INF tcti
		join
		<if test="find == 'flow'">
			TB_CTR_TNK_RST tctr on UPPER(tcti.DSTRB_Q_ID) = UPPER(tctr.DSTRB_ID)
		</if>
		<if test="find == 'press'">
			TB_CTR_TNK_RST tctr on UPPER(tcti.DSTRB_P_ID) = UPPER(tctr.DSTRB_ID)
		</if>
		WHERE
		tcti.TYPE = 0
		AND tctr.MODEL_NAME = 'XGBoost'
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
		tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
		AND
		tctr.RGSTR_TIME &gt;= DATE_SUB(DATE_ADD(#{startDate}, interval 5 minute), INTERVAL #{range} hour)
		AND
		MINUTE(tctr.RGSTR_TIME) % #{cycle} = 0
		) AS combined_results
		ORDER BY ts;
	</select>
	
	<select id="selectCurTnkFlowPressure" resultType="hashmap" parameterType="hashmap">
		SELECT * FROM (
		SELECT
			DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts,
			tr.VALUE as value,
			tcti.SRTTN as srttn
		from
			TB_CTR_TNK_INF tcti
		join
			<if test="find == 'flow'">
				TB_RAWDATA tr on tcti.FRI_TAG = tr.TAGNAME
			</if>
			<if test="find == 'press'">
				TB_RAWDATA tr on tcti.PRI_TAG = tr.TAGNAME
			</if>
		WHERE
			tcti.TYPE = #{type}
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
			tr.TS &lt;= #{startDate}
		AND
			tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		AND
			MINUTE(tr.TS) % #{cycle} = 0
		UNION ALL
		SELECT
		DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts,
		tr.VALUE as value,
		tcti.SRTTN as srttn
		from
		TB_CTR_TNK_INF tcti
		join
		<if test="find == 'flow'">
			TB_RAWDATA tr on tcti.FRI_TAG = tr.TAGNAME
		</if>
		<if test="find == 'press'">
			TB_RAWDATA tr on tcti.PRI_TAG = tr.TAGNAME
		</if>
		WHERE
		tcti.TYPE = 0
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
		tr.TS &lt;= #{startDate}
		AND
		tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
		AND
		MINUTE(tr.TS) % #{cycle} = 0
		) AS combined_results
		ORDER BY ts;
	</select>
	
	<select id="selectPreEPANETTnkFlowPressure" resultType="hashmap" parameterType="hashmap">
		SELECT * FROM (
		SELECT
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
		<if test="find == 'flow'">
			tctr.FLW_ALG_RST_VAL
		</if>
		<if test="find == 'press'">
			tctr.FP_ALG_RST_VAL
		</if>
		 as value,
		tcti.SRTTN as srttn
		from
		TB_CTR_TNK_INF tcti
		join
			<if test="find == 'flow'">
				TB_FR_VAL tctr on tcti.LINK_ID = tctr.LINK_ID
				WHERE
						tcti.FRI_TAG IS NOT NULL
					<if test="notSrttn != null">
						AND
						UPPER(tcti.DSTRB_Q_ID) NOT IN
						<foreach item="name" collection="notSrttn" open="(" separator="," close=")">
							UPPER(#{name})
						</foreach>
					</if>
					AND
			</if>
			<if test="find == 'press'">
				TB_FP_VAL tctr on UPPER(tcti.NODE_ID) = UPPER(tctr.NODE_ID)
				WHERE
						tcti.PRI_TAG IS NOT NULL
					<if test="notSrttn != null">
						AND
						UPPER(tcti.DSTRB_P_ID) NOT IN
						<foreach item="name" collection="notSrttn" open="(" separator="," close=")">
							UPPER(#{name})
						</foreach>
					</if>
					AND
			</if>
			tctr.FLG = 'PRE'
		AND
			tcti.TYPE = 1
		AND
			tcti.NODE_ID IS NOT NULL
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
			tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
		AND
			tctr.RGSTR_TIME &gt;= DATE_SUB(DATE_ADD(#{startDate}, interval 1 minute), INTERVAL #{range} hour)
		AND
			MINUTE(tctr.RGSTR_TIME) % #{cycle} = 0
		UNION ALL
		SELECT
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
		<if test="find == 'flow'">
			tctr.FLW_ALG_RST_VAL
		</if>
		<if test="find == 'press'">
			tctr.FP_ALG_RST_VAL
		</if>
		as value,
		tcti.SRTTN as srttn
		from
		TB_CTR_TNK_INF tcti
		join
		<if test="find == 'flow'">
			TB_FR_VAL tctr on tcti.LINK_ID = tctr.LINK_ID
			WHERE
			tcti.FRI_TAG IS NOT NULL
			<if test="notSrttn != null">
				AND
				UPPER(tcti.DSTRB_Q_ID) NOT IN
				<foreach item="name" collection="notSrttn" open="(" separator="," close=")">
					UPPER(#{name})
				</foreach>
			</if>
			AND
		</if>
		<if test="find == 'press'">
			TB_FP_VAL tctr on UPPER(tcti.NODE_ID) = UPPER(tctr.NODE_ID)
			WHERE
			tcti.PRI_TAG IS NOT NULL
			<if test="notSrttn != null">
				AND
				UPPER(tcti.DSTRB_P_ID) NOT IN
				<foreach item="name" collection="notSrttn" open="(" separator="," close=")">
					UPPER(#{name})
				</foreach>
			</if>
			AND
		</if>
		tctr.FLG = 'PRE'
		AND
		tcti.TYPE = 0
		AND
		tcti.NODE_ID IS NOT NULL
		<if test="pump_grp != null">
			AND
				tcti.USE_YN IN (#{pump_grp}, 0)
		</if>
		AND
		tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
		AND
		tctr.RGSTR_TIME &gt;= DATE_SUB(DATE_ADD(#{startDate}, interval 1 minute), INTERVAL #{range} hour)
		AND
		MINUTE(tctr.RGSTR_TIME) % #{cycle} = 0
		) AS combined_results
		ORDER BY ts;
	</select>
	
	<select id="selectHeadLossTargetCurFlow" resultType="Double" parameterType="hashmap">
		SELECT
		sum(tr.VALUE) as value
		from
		TB_CTR_TNK_INF tcti
		join
			TB_RAWDATA tr on tcti.FRI_TAG = tr.TAGNAME
		WHERE
		UPPER(tcti.DSTRB_Q_ID) in
		<foreach item="id" collection="dstrbList" open="(" separator="," close=")">
			UPPER(#{id})
		</foreach>
		<if test="first == true">
			and tr.TS &lt;= #{startDate}
			and tr.TS &gt; DATE_SUB(#{startDate}, INTERVAL 5 minute)
			group by tr.TS
			order by tr.TS desc limit 1;
		</if>
		<if test="first != true">
			and tr.TS &lt; DATE_SUB(#{startDate}, INTERVAL #{cycle} minute)
			and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
			and MINUTE(tr.TS) % #{cycle} = 0
			group by tr.TS
			order by tr.TS;
		</if>
	</select>
	<select id="selectHeadLossTargetPreFlow" resultType="Double" parameterType="hashmap">
		SELECT
		sum(tctr.PRDCT_VALUE) as value
		from
		TB_CTR_TNK_INF tcti
		join
			TB_CTR_TNK_RST tctr on tcti.DSTRB_Q_ID = tctr.DSTRB_ID
		WHERE
		tctr.MODEL_NAME = 'XGBoost'
		AND UPPER(tcti.DSTRB_Q_ID) in
		<foreach item="id" collection="dstrbList" open="(" separator="," close=")">
			UPPER(#{id})
		</foreach>
		AND
		<if test="first == true">
			tctr.RGSTR_TIME &lt;= DATE_ADD(#{startDate}, interval 1 minute)
			and
			tctr.RGSTR_TIME &gt; DATE_SUB(DATE_SUB(#{startDate}, interval 5 minute), INTERVAL #{cycle} minute)
			group by tctr.RGSTR_TIME
			order by
			tctr.RGSTR_TIME desc limit 1
		</if>
		<if test="first != true">
			tctr.RGSTR_TIME &lt;= DATE_SUB(#{startDate}, INTERVAL #{cycle} minute)
			and
			tctr.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
			and
			MINUTE(tctr.RGSTR_TIME) % #{cycle} = 0
			group by tctr.RGSTR_TIME
			order by
			tctr.RGSTR_TIME
		</if>
	</select>
	
	<insert id="insertDrvnPumpYnData" parameterType="hashmap">
		INSERT INTO TB_CTR_PUMPYN_RST
		(
			OPT_IDX,
			PUMP_GRP,
			PUMP_IDX,
			PUMP_TYP,
			PUMP_YN,
			FREQ,
			RGSTR_TIME,
			PRDCT_MEAN,
			TUBE_PRSR_PRDCT,
			PWR_PRDCT
			<if test="wpp_code == 'gu'">
				,CHNG_STTS
			</if>
		)
		VALUES
		(
			#{opt_idx},
			#{PUMP_GRP},
			#{PUMP_IDX},
			#{PUMP_TYP},
			#{pump_yn},
			#{freq},
			#{ts},
			#{flow},
			#{pressure},
			#{pwrPrdct}
			<if test="wpp_code == 'gu'">
				,#{chng_stts}
			</if>
		)
		ON DUPLICATE KEY UPDATE
		PUMP_YN = #{pump_yn},
		PUMP_TYP = #{PUMP_TYP},
		PRDCT_MEAN = #{flow},
		TUBE_PRSR_PRDCT = #{pressure},
		PWR_PRDCT = #{pwrPrdct},
		FREQ = #{freq}
		<if test="wpp_code == 'gu'">
		,CHNG_STTS = #{chng_stts}
		</if>;
	</insert>

	<!--
	  5단계 horizon 펌프 예측 조합 INSERT.
	  insertDrvnPumpYnData (TB_CTR_PUMPYN_RST) 와 동일 형식 + PRDCT_TIME, HORIZON_MIN 추가.
	  RGSTR_TIME 은 산출 시점(now floor minute), ts 는 예측 대상 시각(PRDCT_TIME).
	-->
	<insert id="insertDrvnPumpYnPrdctData" parameterType="hashmap">
		INSERT INTO TB_CTR_PUMPYN_PRDCT_RST
		(
			OPT_IDX, PUMP_GRP, PUMP_IDX, PUMP_TYP, PUMP_YN, FREQ,
			RGSTR_TIME, PRDCT_TIME, HORIZON_MIN,
			PRDCT_MEAN, TUBE_PRSR_PRDCT, PWR_PRDCT
		)
		VALUES
		(
			#{opt_idx}, #{PUMP_GRP}, #{PUMP_IDX}, #{PUMP_TYP}, #{pump_yn}, #{freq},
			#{rgstrTime}, #{ts}, #{horizonMin},
			#{flow}, #{pressure}, #{pwrPrdct}
		)
		ON DUPLICATE KEY UPDATE
		PUMP_YN = #{pump_yn},
		PUMP_TYP = #{PUMP_TYP},
		PRDCT_TIME = #{ts},
		PRDCT_MEAN = #{flow},
		TUBE_PRSR_PRDCT = #{pressure},
		PWR_PRDCT = #{pwrPrdct},
		FREQ = #{freq}
	</insert>

	<!--
	  펌프대수차트 미래 segment 용. 각 horizon(10/60/120/180/360) 별로
	  가장 최근 RGSTR_TIME 산출 결과의 펌프별 ON/OFF 를 반환.
	  가중치 합산은 서비스(DrvnService.buildForecastList) 에서 수행.
	-->
	<select id="selectPumpRunCountForecast" parameterType="hashmap" resultType="hashmap">
		SELECT
			p.HORIZON_MIN,
			DATE_FORMAT(p.PRDCT_TIME, '%Y-%m-%d %H:%i') AS PRDCT_TIME,
			p.PUMP_IDX,
			p.PUMP_YN,
			p.PUMP_TYP,
			p.FREQ
		FROM TB_CTR_PUMPYN_PRDCT_RST p
		JOIN (
			SELECT HORIZON_MIN, MAX(RGSTR_TIME) AS MAX_TS
			FROM TB_CTR_PUMPYN_PRDCT_RST
			WHERE PUMP_GRP = #{PUMP_GRP}
			  AND RGSTR_TIME &lt;= #{nowDateTime}
			GROUP BY HORIZON_MIN
		) latest
		  ON latest.HORIZON_MIN = p.HORIZON_MIN
		 AND latest.MAX_TS = p.RGSTR_TIME
		WHERE p.PUMP_GRP = #{PUMP_GRP}
	</select>

	<insert id="insertInQuiryPumpYnData" parameterType="hashmap">
		INSERT INTO TB_CTR_PUMPYN_INQUIRY
			(
				OPT_IDX,
				PUMP_GRP,
				PUMP_IDX,
				PUMP_TYP,
				PUMP_YN,
				FREQ,
				RATE_CTGRY,
				FLOW_CTR,
				RGSTR_TIME,
				PRDCT_MEAN,
				TUBE_PRSR_PRDCT,
				PWR_PRDCT
				<if test="wpp_code == 'gu'">
					,CHNG_STTS
				</if>
			)
		VALUES
			(
				#{opt_idx},
				#{PUMP_GRP},
				#{PUMP_IDX},
				#{PUMP_TYP},
				#{pump_yn},
				#{freq},
				#{RATE_CTGRY},
				#{FLOW_CTR},
				#{ts},
				#{flow},
				#{pressure},
				#{pwrPrdct}
				<if test="wpp_code == 'gu'">
					,#{chng_stts}
				</if>
			)
		ON DUPLICATE KEY UPDATE
			PUMP_YN = #{pump_yn},
			RATE_CTGRY = #{RATE_CTGRY},
			FLOW_CTR = #{FLOW_CTR},
			PUMP_TYP = #{PUMP_TYP},
			PRDCT_MEAN = #{flow},
			TUBE_PRSR_PRDCT = #{pressure},
			PWR_PRDCT = #{pwrPrdct},
			FREQ = #{freq}
			<if test="wpp_code == 'gu'">
				,CHNG_STTS = #{chng_stts}
			</if>;
	</insert>
	
	<delete id="deleteInquiryData" parameterType="hashmap">
		DELETE FROM TB_CTR_PUMPYN_INQUIRY
		WHERE
		RGSTR_TIME &gt;= #{startDate}
		AND
		RGSTR_TIME &lt;= #{endDate}
	</delete>
	
	<select id="getLoadInquiry" resultType="hashmap">
		SELECT
			MNTH,
			STN_TM,
			TIMEZONE
		FROM
			TB_RT_RATE_INF
		WHERE
			RATE_IDX = 1
		ORDER BY MNTH, STN_TM;
	</select>
	
	<select id="getInsertUsePrdctFlowPressure" parameterType="hashmap" resultType="hashmap">
		SELECT
		tctr.DSTRB_ID as junction_id,
		tctr.PRDCT_VALUE as value,
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts
		from
		TB_CTR_TNK_RST tctr
		WHERE
				UPPER(tctr.DSTRB_ID) = UPPER(#{DSTRB_ID})
			and
				tctr.MODEL_NAME = 'XGBoost'
			and
				tctr.RGSTR_TIME &lt;= #{endDate}
			and
				tctr.RGSTR_TIME &gt;= #{startDate}
			order by
				tctr.RGSTR_TIME
	</select>
	<select id="getInsertGsFlowPressure" parameterType="hashmap" resultType="hashmap">
		SELECT
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
		MAX(CASE WHEN UPPER(tctr.DSTRB_ID) = UPPER('Q_GS_OLD_Predict') THEN tctr.PRDCT_VALUE END) as flow1,
		MAX(CASE WHEN UPPER(tctr.DSTRB_ID) = UPPER('P_GS_OLD_Predict') THEN tctr.PRDCT_VALUE END) as pressure1,
		MAX(CASE WHEN UPPER(tctr.DSTRB_ID) = UPPER('Q_GS_NEW_Predict') THEN tctr.PRDCT_VALUE END) as flow2,
		MAX(CASE WHEN UPPER(tctr.DSTRB_ID) = UPPER('P_GS_NEW_Predict') THEN tctr.PRDCT_VALUE END) as pressure2
		FROM
		TB_CTR_TNK_RST tctr
		WHERE
		UPPER(tctr.DSTRB_ID) IN (
		'Q_GS_OLD_Predict', 'P_GS_OLD_Predict',
		'Q_GS_NEW_Predict', 'P_GS_NEW_Predict'
		)
		AND tctr.MODEL_NAME = 'XGBoost'
		AND tctr.RGSTR_TIME &lt;= #{endDate}
		AND tctr.RGSTR_TIME &gt;= #{startDate}
		GROUP BY
		DATE_FORMAT(tctr.RGSTR_TIME, '%Y-%m-%d %H:%i')
		ORDER BY
		ts;
	</select>
	
	<select id="selectPrdctPumpCombYn" resultType="hashmap" parameterType="hashmap">
		SELECT
			COALESCE(inquiry.PUMP_GRP, rst.PUMP_GRP) AS PUMP_GRP,
			DATE_FORMAT(rst.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
			COALESCE(inquiry.PUMP_IDX, rst.PUMP_IDX) AS PUMP_IDX,
			COALESCE(inquiry.PUMP_TYP, rst.PUMP_TYP) AS PUMP_TYP,
			COALESCE(inquiry.PUMP_YN, rst.PUMP_YN) AS PUMP_YN,
			COALESCE(inquiry.FREQ, rst.FREQ) AS FREQ
		FROM
			TB_CTR_PUMPYN_RST AS rst
				LEFT JOIN
			TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_IDX = inquiry.PUMP_IDX AND rst.PUMP_GRP = inquiry.PUMP_GRP
			WHERE
			<if test="first == true">
				rst.RGSTR_TIME = (
					select min(RGSTR_TIME)
					from TB_CTR_PUMPYN_RST
					where DATE_ADD(RGSTR_TIME, interval 10 minute) &gt;= #{startDate}
				)
			</if>
		<if test="first != true">
			rst.RGSTR_TIME &lt;= #{startDate}
				AND
			rst.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{range} hour)
				AND
			MINUTE(rst.RGSTR_TIME) % #{cycle} = 0
		</if>
		<if test="pump_grp != 0">
			AND
				rst.PUMP_GRP = #{pump_grp}
		</if>
		ORDER BY
			rst.RGSTR_TIME,
			COALESCE(inquiry.PUMP_GRP, rst.PUMP_GRP) ASC,
			COALESCE(inquiry.PUMP_IDX, rst.PUMP_IDX) ASC;
	</select>
	
	<select id="selectGrpPrdctPumpCombYn" resultType="hashmap" parameterType="hashmap">
		SELECT
			COALESCE(inquiry.PUMP_GRP, rst.PUMP_GRP) AS PUMP_GRP,
			DATE_FORMAT(rst.RGSTR_TIME, '%Y-%m-%d %H:%i') as ts,
			COALESCE(inquiry.PUMP_IDX, rst.PUMP_IDX) AS PUMP_IDX,
			COALESCE(inquiry.PUMP_TYP, rst.PUMP_TYP) AS PUMP_TYP,
			COALESCE(inquiry.PUMP_YN, rst.PUMP_YN) AS PUMP_YN,
			COALESCE(inquiry.FREQ, rst.FREQ) AS FREQ,
			COALESCE(inquiry.PWR_PRDCT, rst.PWR_PRDCT) AS PWR_PRDCT,
			CASE
				WHEN inquiry.OPT_IDX IS NOT NULL THEN 1
				ELSE 0
			END AS flag,
			inquiry.FLOW_CTR as FLOW_CTR
		FROM
			TB_CTR_PUMPYN_RST AS rst
		LEFT JOIN
			TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_IDX = inquiry.PUMP_IDX and rst.PUMP_GRP = inquiry.PUMP_GRP
			WHERE
			<if test="pump_grp != 0">
				rst.PUMP_GRP = #{pump_grp}
				AND
			</if>
				rst.RGSTR_TIME = (
					select min(RGSTR_TIME)
					from TB_CTR_PUMPYN_RST
					where DATE_ADD(RGSTR_TIME, interval 10 minute) &gt;= #{startDate}
				)
			GROUP BY rst.PUMP_IDX
		ORDER BY
			rst.RGSTR_TIME,
			COALESCE(inquiry.PUMP_IDX, rst.PUMP_IDX) ASC;
	</select>
	<select id="optPumpUse" parameterType="hashmap" resultType="int">
		SELECT
		tr.VALUE as value
		FROM
		TB_RAWDATA tr,
		TB_CTR_PRF_PUMPMST_INF tcppi
		WHERE
		tr.TAGNAME = tcppi.PMB_TAG
		and
		tcppi.PUMP_IDX = #{pump_idx}
		and tr.TS &lt;= DATE_SUB(#{startDate}, INTERVAL 1 minute)
		and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL 35 minute)
		order by tr.TS desc
		limit 30;
	</select>
	<select id="optPrePumpUse" parameterType="hashmap" resultType="int">
		SELECT
		CAST(COALESCE(t2.PUMP_YN, t1.PUMP_YN) as INT) AS value
		FROM
		TB_CTR_PUMPYN_RST t1
		LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
		ON t1.OPT_IDX = t2.OPT_IDX
		AND t1.PUMP_IDX = t2.PUMP_IDX
		AND t1.RGSTR_TIME = t2.RGSTR_TIME
		WHERE
		t1.PUMP_IDX = #{pump_idx}
		AND
		t1.RGSTR_TIME &lt;= DATE_SUB(#{startDate}, INTERVAL 1 minute)
		<if test="local != 'wm' and local != 'gs' and local != 'ba' and local != 'gr' and local != 'custom'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 35 minute)
			order by t1.RGSTR_TIME desc
			limit 30;
		</if>
		<if test="local == 'gs'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 125 minute)
			order by t1.RGSTR_TIME desc
			limit 60;
		</if>
		<if test="local == 'gr'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 125 minute)
			order by t1.RGSTR_TIME desc
			limit 120;
		</if>
		<if test="local == 'wm'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 65 minute)
			order by t1.RGSTR_TIME desc
			limit 60;
		</if>
		<if test="local == 'ba'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 125 minute)
			order by t1.RGSTR_TIME desc
			limit 120;
		</if>
		<if test="local == 'custom'">
			and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL #{interval} minute)
			order by t1.RGSTR_TIME desc
			limit #{limit};
		</if>
	</select>
	<select id="optPumpFreq" parameterType="hashmap" resultType="int">
		SELECT
		round(tr.VALUE) as value
		FROM
		TB_RAWDATA tr,
		TB_CTR_PRF_PUMPMST_INF tcppi
		WHERE
		tr.TAGNAME = tcppi.SPI_TAG
		and
		tcppi.PUMP_IDX = #{pump_idx}
		AND
		tcppi.PUMP_GRP = #{pump_grp}
		and tr.TS &lt;= DATE_SUB(#{startDate}, INTERVAL 1 minute)
		and tr.TS &gt;= DATE_SUB(#{startDate}, INTERVAL 35 minute)
		order by tr.TS desc
		limit 30;
	</select>
	
	
	<select id="excelCurPumpPower" resultType="hashmap" parameterType="hashmap">
		SELECT
		DATE_FORMAT(TS, '%Y-%m-%d %H:%i') as TS,
		SUM(VALUE) AS value
		FROM
		TB_RAWDATA_15MIN
		WHERE
		tagname IN (SELECT PWI_TAG FROM TB_CTR_PRF_PUMPMST_INF where PUMP_GRP=#{pump_grp})
		AND TS &gt;= CONCAT(#{startDate}, ' 00:00:00') AND TS &lt;= CONCAT(#{endDate}, ' 23:59:59')
		GROUP BY
		TS
		ORDER BY
		TS
	</select>
	
	<select id="excelPrePumpData" parameterType="hashmap" resultType="hashmap">
		SELECT *
		FROM (
		SELECT
			DATE_FORMAT(COALESCE(inquiry.RGSTR_TIME, rst.RGSTR_TIME), '%Y-%m-%d %H:%i') as TS,
			COALESCE(inquiry.PRDCT_MEAN, rst.PRDCT_MEAN) AS PRDCT_MEAN,
			COALESCE(inquiry.TUBE_PRSR_PRDCT, rst.TUBE_PRSR_PRDCT) AS TUBE_PRSR_PRDCT,
			COALESCE(inquiry.PWR_PRDCT, rst.PWR_PRDCT) AS PWR_PRDCT
			FROM
			TB_CTR_PUMPYN_RST AS rst
			LEFT JOIN
			TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_IDX = inquiry.PUMP_IDX
			WHERE
			rst.PUMP_GRP = #{pump_grp}
			and
			rst.RGSTR_TIME &gt;= CONCAT(#{startDate}, ' 00:00:00')
			AND
			rst.RGSTR_TIME &lt;= CONCAT(#{endDate}, ' 23:59:59')
			GROUP BY rst.RGSTR_TIME
			ORDER BY rst.RGSTR_TIME
		) AS subquery
		WHERE MINUTE(TS) % 15 = 0
		ORDER BY TS;
	</select>
	<select id="excelCurPumpFlow" resultType="hashmap" parameterType="hashmap">
		SELECT
		SUM(VALUE) AS value
		FROM
		TB_RAWDATA_15MIN
		WHERE
		tagname IN (SELECT FRI_TAG FROM TB_CTR_PRF_PUMPMST_INF where PUMP_GRP=#{pump_grp})
		AND TS &gt;= CONCAT(#{startDate}, ' 00:00:00') AND TS &lt;= CONCAT(#{endDate}, ' 23:59:59')
		GROUP BY
		TS
		ORDER BY
		TS
	</select>
	<select id="excelCurPumpPri" parameterType="hashmap" resultType="hashmap">
		SELECT
		SUM(VALUE) AS value
		FROM
		TB_RAWDATA_15MIN
		WHERE
		tagname IN (SELECT PRI_T_TAG FROM TB_CTR_PRF_PUMPMST_INF where PUMP_GRP=#{pump_grp})
		AND TS &gt;= CONCAT(#{startDate}, ' 00:00:00') AND TS &lt;= CONCAT(#{endDate}, ' 23:59:59')
		GROUP BY
		TS
		ORDER BY
		TS
	</select>
	<select id="excelPrePumpYn" resultType="hashmap" parameterType="hashmap">
		SELECT *
		FROM (
			SELECT
			DATE_FORMAT(COALESCE(inquiry.RGSTR_TIME, rst.RGSTR_TIME), '%Y-%m-%d %H:%i') as TS,
			COALESCE(inquiry.PUMP_YN, rst.PUMP_YN) AS value
			FROM
			TB_CTR_PUMPYN_RST AS rst
			LEFT JOIN
			TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_IDX = inquiry.PUMP_IDX
			WHERE
			rst.PUMP_IDX = #{pump_idx}
			and
			rst.PUMP_GRP = #{pump_grp}
			and
			rst.RGSTR_TIME &gt;= CONCAT(#{startDate}, ' 00:00:00')
			AND
			rst.RGSTR_TIME &lt;= CONCAT(#{endDate}, ' 23:59:59')
			GROUP BY rst.RGSTR_TIME
			ORDER BY rst.RGSTR_TIME
		) AS subquery
		WHERE MINUTE(TS) % 15 = 0
		ORDER BY TS;
	</select>
	<select id="excelCurPumpYn" resultType="hashmap" parameterType="hashmap">
		select
			value
		from TB_RAWDATA_15MIN rdh,
			TB_CTR_PRF_PUMPMST_INF cppi
		where
			rdh.TAGNAME = cppi.PMB_TAG
		and
			cppi.PUMP_GRP = #{pump_grp}
		and
			cppi.PUMP_IDX = #{pump_idx}
		AND
			rdh.TS &gt;= CONCAT(#{startDate}, ' 00:00:00')
		and
			rdh.TS &lt;= CONCAT(#{endDate}, ' 23:59:59')
		ORDER BY rdh.TS
	</select>
	
	<select id="getCurUsePumpString" parameterType="hashmap" resultType="hashmap">
		SELECT
			GROUP_CONCAT(
				CASE
					WHEN tr.VALUE = 1 AND tr.TAGNAME = tc.PMB_TAG THEN tc.PUMP_IDX
				END
				ORDER BY tc.PUMP_IDX ASC SEPARATOR ','
			) AS PUMP_USE_RST,
			GROUP_CONCAT(
				CASE
					WHEN tr.TAGNAME = tc.SPI_TAG AND ROUND(tr.VALUE) != 0 THEN ROUND(tr.VALUE)
				END
				ORDER BY tc.PUMP_IDX ASC SEPARATOR ','
			) AS SPI_USE_RST
		FROM
			TB_RAWDATA tr
			JOIN TB_CTR_PRF_PUMPMST_INF tc
			ON tr.TAGNAME = tc.PMB_TAG
			OR tr.TAGNAME = tc.SPI_TAG
		WHERE
			tr.TS = (select max(TS) from TB_RAWDATA where TS &lt;= #{targetDate} and TS &gt; DATE_SUB(#{targetDate}, interval 10 minute))
			<if test="pump_grp != 0">
				AND
				tc.PUMP_GRP = #{pump_grp}
			</if>
		GROUP BY
			tr.TS;
	</select>
	<select id="getPreUsePumpString" parameterType="hashmap" resultType="hashmap">
		SELECT
			GROUP_CONCAT(CASE WHEN A.PUMP_YN = 1 THEN A.PUMP_IDX END ORDER BY A.PUMP_IDX ASC) AS PUMP_USE_RST,
			GROUP_CONCAT(CASE WHEN A.FREQ != 0 THEN ROUND(A.FREQ) END ORDER BY A.FREQ ASC) AS SPI_USE_RST
		FROM (
			SELECT
				t1.PUMP_IDX,
				COALESCE(t2.PUMP_YN, t1.PUMP_YN) AS PUMP_YN,
				COALESCE(t2.RGSTR_TIME, t1.RGSTR_TIME) AS RGSTR_TIME,
				COALESCE(t2.FREQ, t1.FREQ) AS FREQ
			FROM
			TB_CTR_PUMPYN_RST t1
			LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
				ON t1.OPT_IDX = t2.OPT_IDX
				AND t1.PUMP_IDX = t2.PUMP_IDX
				AND t1.RGSTR_TIME = t2.RGSTR_TIME
			WHERE
				<if test="pump_grp != 0">
						t1.PUMP_GRP = #{pump_grp}
					AND
				</if>
				t1.RGSTR_TIME = (select max(RGSTR_TIME) from TB_CTR_PUMPYN_RST where RGSTR_TIME &lt;= DATE_SUB(#{targetDate}, interval 1 minute) and RGSTR_TIME &gt; DATE_SUB(#{targetDate}, interval 30 minute))
		) A
		GROUP BY
			A.RGSTR_TIME;
	</select>
	
	<sql id="baseDateData">
<!--		WITH RECURSIVE A AS (-->
<!--			SELECT STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:00') AS TS-->
<!--			UNION ALL-->
<!--			SELECT DATE_ADD(A.TS, INTERVAL 15 MINUTE) AS TS FROM A-->
<!--			WHERE A.TS &lt;= STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:00')-->
<!--		)-->
		WITH RECURSIVE A AS (
			SELECT STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:00') AS TS
			UNION ALL
			SELECT DATE_ADD(A.TS, INTERVAL 15 MINUTE)
			FROM A
			WHERE A.TS &lt; STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:00')
		)
	</sql>
	<select id="pumpInstPwr" resultType="double" parameterType="hashmap">
		<include refid="baseDateData"/>
		SELECT
			SUM(tr.VALUE * tpti.PWI_UNIT_VALUE) AS pwr
		FROM
			A i
			LEFT JOIN
			TB_RAWDATA tr ON i.TS = tr.TS
			LEFT JOIN
			TB_CTR_PRF_PUMPMST_INF tcppi ON tr.TAGNAME = tcppi.PWI_TAG
			LEFT JOIN
			TB_PEAK_TAG_INF tpti ON tr.TAGNAME = tpti.PWI_TAG
		WHERE
			tcppi.PUMP_GRP = #{pump_grp} OR tr.TS IS NULL
		GROUP BY
			i.TS
		ORDER BY
			i.TS;
	</select>
	<select id="pumpInstFlowRate" resultType="double" parameterType="hashmap">
		<include refid="baseDateData"/>
		SELECT
			tr.VALUE as flow
		FROM
			A i
			LEFT JOIN
				TB_RAWDATA tr ON i.TS = tr.TS
			LEFT JOIN
				TB_CTR_PRF_PUMPMST_INF tcppi ON tr.TAGNAME = tcppi.FRI_TAG
		WHERE
			tcppi.PUMP_GRP = #{pump_grp} OR tr.TS IS NULL
		GROUP BY
			i.TS
		ORDER BY
			i.TS;
	</select>
	<select id="pumpPrdcPwrFlow" resultType="hashmap" parameterType="hashmap">
		<include refid="baseDateData"/>
		SELECT
			i.TS,
			COALESCE(t2.PWR_PRDCT, t1.PWR_PRDCT) AS pwr,
			COALESCE(t2.PRDCT_MEAN, t1.PRDCT_MEAN) AS flow
		FROM
		A i
		LEFT JOIN
		TB_CTR_PUMPYN_RST t1 ON i.TS = t1.RGSTR_TIME
		LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
		ON
		t1.OPT_IDX = t2.OPT_IDX
		AND
		t1.PUMP_IDX = t2.PUMP_IDX
		AND
		t1.RGSTR_TIME = t2.RGSTR_TIME
		WHERE
		t1.PUMP_GRP = #{pump_grp} OR t1.RGSTR_TIME IS NULL
		GROUP BY
		i.TS
		ORDER BY
		i.TS
	</select>
	<select id="pumpActlMsrmCmbn" resultType="hashmap" parameterType="hashmap">
		<include refid="baseDateData"/>
		SELECT
			i.TS,
			GROUP_CONCAT(DISTINCT CASE WHEN tr.VALUE = 1 THEN tc.PUMP_IDX END ORDER BY tc.PUMP_IDX ASC) AS PUMP_YN_RST
		FROM
			A i
			LEFT JOIN
			TB_RAWDATA tr ON i.TS = tr.TS
		JOIN
			TB_CTR_PRF_PUMPMST_INF tc
			ON
			tr.TAGNAME = tc.PMB_TAG
		WHERE 1 = 1
			<if test="pump_grp != 0">
				AND
					tc.PUMP_GRP = #{pump_grp}
			</if>
			OR
				tr.TS IS NULL
		GROUP BY
			i.TS
		ORDER BY
			i.TS
	</select>
	<select id="pumpPrdcCmbn" resultType="hashmap" parameterType="hashmap">
		<include refid="baseDateData"/>
		,
		CTE AS (
			SELECT
				i.TS AS RGSTR_TIME,
				t1.PUMP_IDX,
				t1.PUMP_GRP,
				COALESCE(t2.PUMP_YN, t1.PUMP_YN) AS PUMP_YN,
				COALESCE(t2.PWR_PRDCT, t1.PWR_PRDCT) AS pwr,
				COALESCE(t2.PRDCT_MEAN, t1.PRDCT_MEAN) AS flow
			FROM
			A i
				LEFT JOIN TB_CTR_PUMPYN_RST t1
					ON i.TS = t1.RGSTR_TIME
					AND t1.PUMP_GRP = #{pump_grp}
				LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
					ON t1.OPT_IDX = t2.OPT_IDX
					AND t1.PUMP_IDX = t2.PUMP_IDX
					AND t1.RGSTR_TIME = t2.RGSTR_TIME
					AND t1.PUMP_GRP = t2.PUMP_GRP
		)
		SELECT
			RGSTR_TIME,
			GROUP_CONCAT(DISTINCT CASE WHEN PUMP_YN = 1 THEN PUMP_IDX END ORDER BY PUMP_IDX ASC) AS PUMP_YN_RST,
			pwr,
			flow
		FROM CTE
			GROUP BY RGSTR_TIME;
	</select>
	
	<select id="getWMFreqIdx" parameterType="String" resultType="int">
		select CONVERT(VALUE, UNSIGNED) FROM TB_RAWDATA WHERE TAGNAME = '565-340-IVC-4001'
		AND
		TS &lt;= #{ts}
		and
		TS &gt;= DATE_SUB(#{ts}, interval 10 minute)
		ORDER BY
		TS desc limit 1
	</select>
	
	<select id="getLastCurFreq" parameterType="hashmap" resultType="double">
		select
			VALUE
		FROM
		TB_RAWDATA tr
		JOIN TB_CTR_PRF_PUMPMST_INF tc
		ON tr.TAGNAME = tc.SPI_TAG
		WHERE
		tr.TS = (select max(TS) from TB_RAWDATA where TS &lt;= DATE_SUB(#{targetDate}, interval 1 minute) and TS &gt; DATE_SUB(#{targetDate}, interval 10 minute))
		AND
		tc.PUMP_IDX = #{pump_idx}
	</select>
	
	<select id="wmInverterChange" parameterType="String" resultType="int">
		select CONVERT(VALUE, UNSIGNED)
		FROM
		TB_RAWDATA
		WHERE TAGNAME = '565-340-IVC-4001'
		AND
		TS &lt;= DATE_SUB(#{targetDate}, INTERVAL 1 minute)
		and
		TS &gt;= DATE_SUB(#{targetDate}, INTERVAL 35 minute)
		order by TS desc
		limit 30;
	</select>
	
	<select id="selectRawData" parameterType="hashmap" resultType="double">
		SELECT
		VALUE
		from
		TB_RAWDATA
		WHERE
		TAGNAME = #{tagname}
		and
		TS &lt;= #{nowDateTime}
		and
		TS &gt;= DATE_SUB(#{nowDateTime}, interval 10 minute)
		ORDER BY
		TS desc limit 1
	</select>

	<!--
		[Seoul/Dev] 여러 태그 × 시간 범위 raw 데이터를 단일 쿼리로 조회.
		서울 부하+수위 로직(L: isStationReachableSinlimStyle, M: predict22LevelSeoul)의
		slot×tag N+1 조회를 1회로 단축.

		파라미터:
		  - tags          : tagname 리스트 (IN 절)
		  - startDateTime : 가장 오래된 slot 의 시각 ("yyyy-MM-dd HH:mm:ss")
		  - endDateTime   : 최신 slot 의 시각 (보통 now)
		범위는 [startDateTime - 10min, endDateTime] 으로 잡아 기존 selectRawData 의
		10분 lookback 호환성을 유지한다. 호출부는 (slot-10min, slot] 윈도우에서
		TS DESC 첫 행을 그 slot 의 활성값으로 채택한다.
	-->
	<select id="selectRawDataRangeForTags" parameterType="hashmap" resultType="hashmap">
		SELECT
			TAGNAME as tagname,
			DATE_FORMAT(TS, '%Y-%m-%d %H:%i:%s') as ts,
			VALUE as value
		FROM TB_RAWDATA
		WHERE TAGNAME IN
		<foreach collection="tags" item="tag" open="(" separator="," close=")">
			#{tag}
		</foreach>
		AND TS &lt;= #{endDateTime}
		AND TS &gt;= DATE_SUB(#{startDateTime}, INTERVAL 10 MINUTE)
		ORDER BY TAGNAME, TS DESC
	</select>
	
	<select id="select5MinuteAvgRawData" parameterType="hashmap" resultType="double">
		SELECT
			AVG(VALUE) as VALUE
		from
			TB_RAWDATA
		WHERE
			TAGNAME = #{tagname}
			and
			TS &lt;= #{nowDateTime}
			and
			TS &gt;= DATE_SUB(#{nowDateTime}, interval 5 minute)
	</select>
	<select id="select10MinuteAvgRawData" parameterType="hashmap" resultType="double">
		SELECT
			AVG(VALUE) as VALUE
		from
			TB_RAWDATA
		WHERE
			TAGNAME = #{tagname}
			and
			TS &lt;= #{nowDateTime}
			and
			TS &gt;= DATE_SUB(#{nowDateTime}, interval 5 minute)
	</select>
	
	<select id="wmInverterPumpFreqCheck" parameterType="hashmap" resultType="int">
		SELECT
		CAST(COALESCE(t2.FREQ, t1.FREQ) as INT) AS value
		FROM
		TB_CTR_PUMPYN_RST t1
		LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
		ON t1.OPT_IDX = t2.OPT_IDX
		AND t1.PUMP_IDX = t2.PUMP_IDX
		AND t1.RGSTR_TIME = t2.RGSTR_TIME
		WHERE
		t1.PUMP_IDX = #{pump_idx}
		AND
		t1.RGSTR_TIME &lt;= DATE_SUB(#{startDate}, INTERVAL 1 minute)
		and t1.RGSTR_TIME &gt;= DATE_SUB(#{startDate}, INTERVAL 65 minute)
		order by t1.RGSTR_TIME desc
		limit 60;
	</select>
	
	<select id="pumpDrvnCombExcelData" parameterType="String" resultType="hashmap">
		SELECT
		DATE_FORMAT(rst.RGSTR_TIME, '%Y-%m-%d %H:%i:%s') as rgstr_time,
		rst.PUMP_YN_RST as 'pre_comb',
		raw.PUMP_USE_RST as 'cur_comb',
		rst.FREQ_VALUES as 'pre_freq',
		raw.SPI_USE_RST as 'cur_freq'
		FROM
		(SELECT
		A.RGSTR_TIME,
		GROUP_CONCAT(CASE WHEN A.PUMP_YN = 1 THEN A.PUMP_IDX END ORDER BY A.PUMP_IDX ASC) AS PUMP_YN_RST,
		GROUP_CONCAT(CASE WHEN A.FREQ != 0 THEN ROUND(A.FREQ) END ORDER BY A.FREQ ASC) AS FREQ_VALUES
		FROM (
		SELECT
		t1.PUMP_IDX,
		COALESCE(t2.PUMP_YN, t1.PUMP_YN) AS PUMP_YN,
		COALESCE(t2.RGSTR_TIME, t1.RGSTR_TIME) AS RGSTR_TIME,
		COALESCE(t2.RATE_CTGRY, 'None') AS RATE_CTGRY,
		COALESCE(t2.FREQ, t1.FREQ) AS FREQ,
		COALESCE(t2.FLOW_CTR, 'None') AS FLOW_CTR,
		CASE
		WHEN t2.OPT_IDX is not null THEN 1
		ELSE 0
		END AS FLAG
		FROM
		TB_CTR_PUMPYN_RST t1
		LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
		ON t1.OPT_IDX = t2.OPT_IDX
		AND t1.PUMP_IDX = t2.PUMP_IDX
		AND t1.RGSTR_TIME = t2.RGSTR_TIME
		WHERE
		t1.RGSTR_TIME &gt;= CONCAT(#{startDate}, ' 00:00:00')
		AND
		t1.RGSTR_TIME &lt;= CONCAT(#{endDate}, ' 23:59:00')
		) A
		GROUP BY
		A.RGSTR_TIME ) rst
		LEFT JOIN
		(SELECT
		tr.TS as TS,
		GROUP_CONCAT(CASE WHEN tr.VALUE = 1 THEN tc.PUMP_IDX END ORDER BY tc.PUMP_IDX ASC) AS PUMP_USE_RST,
		GROUP_CONCAT(
		CASE
		WHEN tr.TAGNAME = tc.SPI_TAG AND ROUND(tr.VALUE) != 0 THEN ROUND(tr.VALUE)
		END
		ORDER BY tc.PUMP_IDX ASC SEPARATOR ','
		) AS SPI_USE_RST
		FROM
		TB_RAWDATA tr
		JOIN TB_CTR_PRF_PUMPMST_INF tc
		ON tr.TAGNAME = tc.PMB_TAG
		OR tr.TAGNAME = tc.SPI_TAG
		WHERE
		tr.TS &gt;= CONCAT(#{startDate}, ' 00:00:00')
		AND
		tr.TS &lt;= CONCAT(#{endDate}, ' 23:59:00')
		GROUP BY
		tr.TS) raw
		ON rst.RGSTR_TIME = raw.TS
		GROUP BY
		rst.RGSTR_TIME
		ORDER BY
		rst.RGSTR_TIME;
	</select>
	
	<select id="getGrLifePre" resultType="hashmap">
		SELECT
			tr.TS,
			CAST(MAX(CASE WHEN tr.TAGNAME = '780-344-PRC-6004' THEN tr.VALUE END) AS DOUBLE) AS `780-344-PRC-6004`,
			CAST(MAX(CASE WHEN tr.TAGNAME = '780-344-PRC-4004' THEN tr.VALUE END) AS DOUBLE) AS `780-344-PRC-4004`
		FROM
			TB_RAWDATA tr
		WHERE
				tr.TAGNAME IN ('780-344-PRC-6004', '780-344-PRC-4004')
			AND tr.TS = (
					SELECT
						MAX(TS)
					FROM
						TB_RAWDATA
					WHERE
						TS &lt;= NOW() AND TS &gt; DATE_SUB(NOW(), INTERVAL 10 MINUTE)
				)
		GROUP BY
			tr.TS;
	</select>
	
	<select id="selectPumpCombPressure" resultType="double" parameterType="hashmap">
		SELECT
			ROUND(CAST(COALESCE(inquiry.TUBE_PRSR_PRDCT, rst.TUBE_PRSR_PRDCT) AS DOUBLE), 1) AS TUBE_PRSR_PRDCT
		FROM
			TB_CTR_PUMPYN_RST AS rst
		LEFT JOIN
			TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_GRP = inquiry.PUMP_GRP AND rst.RGSTR_TIME = inquiry.RGSTR_TIME
		WHERE
			rst.RGSTR_TIME &lt;= #{nowDate}
		AND
			rst.RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL #{interval} MINUTE)
		AND
			rst.PUMP_GRP = #{pumpGrp}
		GROUP BY
			rst.RGSTR_TIME
		ORDER BY
			rst.RGSTR_TIME DESC
		LIMIT #{limit};
	</select>
	
	<select id="checkManualOperLog" parameterType="hashmap" resultType="int">
		SELECT
			COUNT(*)
		FROM
			TB_MNL_CHN_LOG
		WHERE
			RGSTR_TIME > DATE_SUB(#{serverTime}, INTERVAL #{interval} MINUTE)
			<if test="pump_grp != 0">
				AND
				PUMP_GRP = #{pump_grp};
			</if>
	</select>
	<select id="checkManualOperLogPump" parameterType="hashmap" resultType="hashmap">
		SELECT
		DATE_FORMAT(RGSTR_TIME, '%Y-%m-%d %H:%i') AS RGSTR_TIME, NewPumpComb, NewPumpFreq, AgoPumpComb, AgoPumpFreq
		FROM
		TB_MNL_CHN_LOG
		WHERE
		<if test="local == 'gs'">
			RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL 60 MINUTE)
		</if>
		<if test="local == 'gr'">
			RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL 60 MINUTE)
		</if>
		<if test="local == 'wm'">
			RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL 60 MINUTE)
		</if>
		<if test="local == 'ba'">
			RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL 30 MINUTE)
		</if>
		AND
		RGSTR_TIME &lt;= #{nowDate}
		<if test="pump_grp != 0">
			AND
			PUMP_GRP = #{pump_grp}
		</if>
		ORDER BY
		RGSTR_TIME DESC
		LIMIT 1;
	</select>
	
	<select id="guPumpStatusChange" resultType="String" parameterType="hashmap">
		SELECT
		COALESCE(inquiry.CHNG_STTS, rst.CHNG_STTS) AS CHNG_STTS
		FROM
		TB_CTR_PUMPYN_RST AS rst
		LEFT JOIN
		TB_CTR_PUMPYN_INQUIRY AS inquiry ON rst.OPT_IDX = inquiry.OPT_IDX AND rst.PUMP_GRP = inquiry.PUMP_GRP AND rst.RGSTR_TIME = inquiry.RGSTR_TIME
		WHERE
		rst.RGSTR_TIME &lt;= #{nowDate}
		AND
		rst.RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL #{interval} MINUTE)
		GROUP BY
		rst.RGSTR_TIME
		ORDER BY
		rst.RGSTR_TIME DESC
		LIMIT #{limit};
	</select>
	<select id="getPreUsePumpStatus" parameterType="hashmap" resultType="hashmap">
		SELECT
			GROUP_CONCAT(CASE WHEN A.PUMP_YN = 1 THEN A.PUMP_IDX END ORDER BY A.PUMP_IDX ASC) AS PUMP_USE_RST,
			GROUP_CONCAT(CASE WHEN A.FREQ != 0 THEN ROUND(A.FREQ) END ORDER BY A.FREQ ASC) AS SPI_USE_RST,
			DATE_FORMAT(A.RGSTR_TIME, '%Y-%m-%d %H:%i') AS RGSTR_TIME,
			A.OPT_IDX AS OPT_IDX,
			CAST(A.TUBE_PRSR_PRDCT AS DOUBLE) AS TUBE_PRSR_PRDCT,
			CAST(A.PRDCT_MEAN AS DOUBLE) AS PRDCT_MEAN
		FROM (
			SELECT
				t1.PUMP_IDX,
				COALESCE(t2.PUMP_YN, t1.PUMP_YN) AS PUMP_YN,
				COALESCE(t2.RGSTR_TIME, t1.RGSTR_TIME) AS RGSTR_TIME,
				COALESCE(t2.FREQ, t1.FREQ) AS FREQ,
				COALESCE(t2.OPT_IDX, t1.OPT_IDX) AS OPT_IDX,
				COALESCE(t2.TUBE_PRSR_PRDCT, t1.TUBE_PRSR_PRDCT) AS TUBE_PRSR_PRDCT,
				COALESCE(t2.PRDCT_MEAN, t1.PRDCT_MEAN) AS PRDCT_MEAN
			FROM
			TB_CTR_PUMPYN_RST t1
				LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
				ON t1.OPT_IDX = t2.OPT_IDX
				AND t1.PUMP_IDX = t2.PUMP_IDX
				AND t1.RGSTR_TIME = t2.RGSTR_TIME
			WHERE
				<if test="pump_grp != 0">
						t1.PUMP_GRP = #{pump_grp}
					AND
				</if>
			t1.RGSTR_TIME = (select max(RGSTR_TIME) from TB_CTR_PUMPYN_RST where RGSTR_TIME &lt;= #{targetDate} and RGSTR_TIME &gt; DATE_SUB(#{targetDate}, interval 10 minute))
		) A
		GROUP BY
			A.RGSTR_TIME;
	</select>
	<insert id="insertManualOperLog" parameterType="hashmap">
		INSERT INTO TB_MNL_CHN_LOG
			(RGSTR_TIME, OPER, PUMP_GRP)
		VALUES(
			#{nowDate},
			#{oper},
			#{pump_grp}
		);
	</insert>

	<!--
	  PUMP_GRP 별 가장 최근 제어 명령 시각 (TB_MNL_CHN_LOG 의 가장 최근 RGSTR_TIME).
	  AI 자동/AI 추천/수동 모든 제어 명령 발사 후 기록되는 통합 lock 시각.
	-->
	<select id="selectLastCtrlTime" parameterType="hashmap" resultType="String">
		SELECT
			DATE_FORMAT(MAX(RGSTR_TIME), '%Y-%m-%d %H:%i') AS LAST_CTRL_TIME
		FROM
			TB_MNL_CHN_LOG
		WHERE
			PUMP_GRP = #{pump_grp}
	</select>

	<!--
	  TB_HMI_CTR_TAG 의 가장 최근 실제 명령 발사 시각 (ANLY_CD IN RUN/STOP/FREQ).
	  멀티 인스턴스(여러 로컬 백엔드가 동일 DB 공유) 환경에서 노드 간 공유 가능한 단일 진실 원천.
	  PUMP_GRP 컬럼이 TB_HMI_CTR_TAG 에 없어 그룹 무관 전체 락 의도.
	-->
	<select id="selectLastHmiCtrTime" resultType="String">
		SELECT
			DATE_FORMAT(MAX(RGSTR_TIME), '%Y-%m-%d %H:%i:%s') AS LAST_HMI_CTR_TIME
		FROM
			TB_HMI_CTR_TAG
		WHERE
			ANLY_CD IN ('RUN', 'STOP', 'FREQ')
	</select>

	<!--
	  펌프 제어 쿨다운(분) 전역 설정값 (TB_WPP_TAG_CODE, FUNC_TYP='CTRL_LOCK_MIN').
	  마지막 제어 후 대기시간. 행 없으면 null → PumpService 에서 하드코딩 fallback.
	-->
	<select id="selectCtrlLockMin" resultType="java.lang.Integer">
		SELECT
			DEFAULT_VALUE
		FROM
			TB_WPP_TAG_CODE
		WHERE
			FUNC_TYP = 'CTRL_LOCK_MIN'
		LIMIT 1
	</select>

	<!--
	  펌프 제어주기(분) 전역 설정값 (tb_ctr_cycle, 전역 단일 행).
	  사용자가 변경 가능. 행 없으면 null → PumpService 에서 TB_WPP_TAG_CODE / 하드코딩 순으로 fallback.
	-->
	<select id="selectCtrlCycleMin" resultType="java.lang.Integer">
		SELECT
			LOCK_MIN
		FROM
			tb_ctr_cycle
		ORDER BY CYCLE_IDX
		LIMIT 1
	</select>

	<!-- 전역 제어주기(분) 변경. 행이 없으면 생성, 있으면 갱신 (CYCLE_IDX=1 고정). -->
	<insert id="upsertCtrlCycleMin" parameterType="hashmap">
		INSERT INTO tb_ctr_cycle (CYCLE_IDX, LOCK_MIN, UPDT_USER)
		VALUES (1, #{lockMin}, #{updtUser})
		ON DUPLICATE KEY UPDATE
			LOCK_MIN = #{lockMin},
			UPDT_USER = #{updtUser}
	</insert>

	<!--
	  펌프 예측 가동이력 타임라인 (예측·실측 비교 차트용).
	  지정 날짜(date)의 최신 분석회차(ANLY_TIME MAX) 예측 결과를 펌프별 PRDCT_TIME 단위로 반환.
	  실측은 기존 /es/selectPumpPerformList(PMB_TAG) 재사용, 본 쿼리는 예측만 담당.
	  name = CONCAT(PUMP_GRP_NM, PUMP_GRP_IDX) → 실측 응답의 name 과 동일 규칙으로 매칭됨.
	-->
	<select id="selectPumpForecastTimeline" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(cor.PRDCT_TIME, '%Y-%m-%d %H:%i') AS ts,
			cpr.PUMP_IDX,
			cppi.PUMP_GRP_IDX,
			CONCAT(cppi.PUMP_GRP_NM, cppi.PUMP_GRP_IDX) AS name,
			MAX(cpr.PUMP_YN) AS PUMP_YN
		FROM TB_CTR_OPT_RST cor
		JOIN TB_CTR_PUMPYN_RST cpr
			ON cor.OPT_IDX = cpr.OPT_IDX
			AND cor.PUMP_GRP = cpr.PUMP_GRP
		JOIN TB_CTR_PRF_PUMPMST_INF cppi
			ON cpr.PUMP_IDX = cppi.PUMP_IDX
		WHERE cor.PUMP_GRP = #{pump_grp}
			AND cor.PRDCT_TIME &gt;= CONCAT(#{date}, ' 00:00:00')
			AND cor.PRDCT_TIME &lt;  CONCAT(#{date}, ' 23:59:59')
			AND cor.ANLY_TIME = (
				SELECT MAX(ANLY_TIME)
				FROM TB_CTR_OPT_RST
				WHERE PUMP_GRP = #{pump_grp}
					AND PRDCT_TIME &gt;= CONCAT(#{date}, ' 00:00:00')
					AND PRDCT_TIME &lt;  CONCAT(#{date}, ' 23:59:59')
			)
		-- (PRDCT_TIME, 펌프)당 1행 보장: PRDCT_TIME_DIFF/FLAG/DC_NMB 등으로 인한 중복 집계 제거
		GROUP BY ts, cpr.PUMP_IDX, cppi.PUMP_GRP_IDX, cppi.PUMP_GRP_NM
		ORDER BY ts, cppi.PUMP_GRP_IDX
	</select>

	<!--
	  펌프 실측 가동이력 타임라인 (예측·실측 비교 차트용, PMB_TAG 전용 경량 쿼리).
	  기존 /es/selectPumpPerformList 가 PMB·PWI·PRI·SPI 4종을 모두 스캔하던 것을,
	  본 차트가 쓰는 PMB(가동여부)만 + 해당 PUMP_GRP 으로 좁혀 비용을 줄임.
	  name = CONCAT(PUMP_GRP_NM, PUMP_GRP_IDX) → 예측 결과 name 과 동일 규칙.
	  value > 0 이면 가동.
	-->
	<select id="selectPumpActualTimeline" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(b.ts, '%Y-%m-%d %H:%i') AS ts,
			cppi.PUMP_GRP_IDX,
			CONCAT(cppi.PUMP_GRP_NM, cppi.PUMP_GRP_IDX) AS name,
			MAX(b.value) AS value
		FROM TB_RAWDATA_HOUR b
		JOIN TB_CTR_PRF_PUMPMST_INF cppi
			ON cppi.PMB_TAG = b.tagname
		WHERE b.ts &gt;= CONCAT(#{date}, ' 00:00:00')
			AND b.ts &lt;= CONCAT(#{date}, ' 23:59:00')
			AND cppi.PUMP_GRP = #{pump_grp}
		GROUP BY b.ts, b.tagname, cppi.PUMP_GRP_IDX, cppi.PUMP_GRP_NM
		ORDER BY b.ts, cppi.PUMP_GRP_IDX
	</select>

	<!--
	  펌프 실측 가동이력 (10분 버킷, 예측·실측 단일차트 비교용).
	  start_date ~ end_date 구간을 TB_RAWDATA(PMB_TAG)에서 10분 경계(:00/:10/.../:50)로 집계.
	  예측(selectPumpForecastByRgstr, RGSTR_TIME 10분단위)과 동일 시간축에 정렬되도록 실측도 10분 버킷.
	  TS - INTERVAL (MINUTE % 10) MINUTE 로 10분 단위로 내림(floor) → 한 버킷 내 한 분이라도 가동이면 MAX(VALUE)>0.
	  응답 스키마 동일({ts,PUMP_GRP_IDX,name,value}). 분단위 대비 결과 행 ~10배 감소(응답·렌더 비용 절감).
	  HAVING 으로 가동(ON) 버킷만 반환.
	-->
	<select id="selectPumpActualMinute" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(b.TS - INTERVAL (MINUTE(b.TS) % 10) MINUTE, '%Y-%m-%d %H:%i') AS ts,
			cppi.PUMP_GRP_IDX,
			CONCAT(cppi.PUMP_GRP_NM, cppi.PUMP_GRP_IDX) AS name,
			MAX(b.VALUE) AS value
		FROM TB_RAWDATA b
		JOIN TB_CTR_PRF_PUMPMST_INF cppi
			ON cppi.PMB_TAG = b.TAGNAME
		WHERE b.TS &gt;= #{start_date}
			AND b.TS &lt;= #{end_date}
			AND cppi.PUMP_GRP = #{pump_grp}
		GROUP BY ts, cppi.PUMP_GRP_IDX, cppi.PUMP_GRP_NM
		HAVING MAX(b.VALUE) &gt; 0
		ORDER BY ts, cppi.PUMP_GRP_IDX
	</select>

	<!--
	  펌프 예측 가동이력 (10분단위, 예측·실측 단일차트 비교용).
	  제어현황 펌프조합 기록(selectPrdctPumpCombYn)과 동일하게 RGSTR_TIME(예측 생성시점) 기준.
	  예측은 10분마다 생성되므로 MINUTE(RGSTR_TIME) % 10 = 0 인 시점만, start_date ~ end_date 구간 반환.
	  프론트 name 매칭을 위해 PUMP_GRP_IDX/name 조인 추가.
	  → "그 시각의 실측에 대해 생성된 예측" 을 동일 시간축 위에서 점으로 대조.
	-->
	<select id="selectPumpForecastByRgstr" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(rst.RGSTR_TIME, '%Y-%m-%d %H:%i') AS ts,
			rst.PUMP_IDX,
			cppi.PUMP_GRP_IDX,
			CONCAT(cppi.PUMP_GRP_NM, cppi.PUMP_GRP_IDX) AS name,
			MAX(rst.PUMP_YN) AS PUMP_YN
		FROM TB_CTR_PUMPYN_RST rst
		JOIN TB_CTR_PRF_PUMPMST_INF cppi
			ON rst.PUMP_IDX = cppi.PUMP_IDX
		WHERE rst.RGSTR_TIME &gt;= #{start_date}
			AND rst.RGSTR_TIME &lt;= #{end_date}
			AND MINUTE(rst.RGSTR_TIME) % 10 = 0
			AND rst.PUMP_GRP = #{pump_grp}
		GROUP BY ts, rst.PUMP_IDX, cppi.PUMP_GRP_IDX, cppi.PUMP_GRP_NM
		HAVING MAX(rst.PUMP_YN) &gt; 0
		ORDER BY ts, cppi.PUMP_GRP_IDX
	</select>

	<!--
	  그룹 내 전체 펌프 명단(roster). 예측·실측 비교 차트의 y축을 고정하기 위함.
	  24시간 내 가동/예측이 전혀 없는 펌프도 빈 행으로 표시되도록 데이터와 무관하게 전체 펌프를 반환.
	-->
	<select id="selectPumpRosterByGrp" parameterType="hashmap" resultType="hashmap">
		SELECT DISTINCT
			cppi.PUMP_GRP_IDX,
			CONCAT(cppi.PUMP_GRP_NM, cppi.PUMP_GRP_IDX) AS name
		FROM TB_CTR_PRF_PUMPMST_INF cppi
		WHERE cppi.PUMP_GRP = #{pump_grp}
		ORDER BY cppi.PUMP_GRP_IDX
	</select>

	<insert id="insertManualOperLogNew" parameterType="hashmap">
		INSERT INTO TB_MNL_CHN_LOG
			(RGSTR_TIME, OPER, PUMP_GRP, NewPumpComb, NewPumpFreq, AgoPumpComb, AgoPumpFreq)
		VALUES(
			#{nowDate},
			#{oper},
			#{pump_grp},
			#{newPumpComb},
			#{newPumpFreq},
			#{agoPumpComb},
			#{agoPumpFreq}
		)
		ON DUPLICATE KEY UPDATE
		NewPumpComb = #{newPumpComb},
		NewPumpFreq = #{newPumpFreq},
		AgoPumpComb = #{agoPumpComb},
		AgoPumpFreq = #{agoPumpFreq};
	</insert>
	<select id="inverterPumpFreqCheck" parameterType="hashmap" resultType="int">
		SELECT
		CAST(COALESCE(t2.FREQ, t1.FREQ) as INT) AS value
		FROM
		TB_CTR_PUMPYN_RST t1
		LEFT JOIN TB_CTR_PUMPYN_INQUIRY t2
		ON t1.OPT_IDX = t2.OPT_IDX
		AND t1.PUMP_IDX = t2.PUMP_IDX
		AND t1.RGSTR_TIME = t2.RGSTR_TIME
		WHERE
		t1.PUMP_IDX = #{pump_idx}
		AND
		t1.PUMP_GRP = #{pump_grp}
		AND
		t1.RGSTR_TIME &lt;= #{nowDate}
		and t1.RGSTR_TIME &gt;= DATE_SUB(#{nowDate}, INTERVAL #{interval} minute)
		order by t1.RGSTR_TIME desc
		limit #{limit};
	</select>
	
	<select id="getBaWppValveData" parameterType="String" resultType="double">
		SELECT
			CAST(DEFAULT_VALUE as DOUBLE) as DEFAULT_VALUE
		FROM
			TB_WPP_TAG_CODE
		WHERE
			FUNC_TYP = #{func_typ};
	</select>
	
	<select id="getWppData" parameterType="String" resultType="hashmap">
		SELECT
			DEFAULT_VALUE as DEFAULT_VALUE,
			TAG_DSC as TAG_DSC,
			TAG as PUMP_COMB
		FROM
			TB_WPP_TAG_CODE
		WHERE
			FUNC_TYP = #{func_typ};
	</select>
	
	<select id="setBaWppValveData" parameterType="hashmap">
		UPDATE
		TB_WPP_TAG_CODE
		SET
			TAG_DSC= #{nowDate},
			DEFAULT_VALUE= #{value}
		WHERE
		FUNC_TYP = #{func_typ};
	</select>
	<select id="setWppOnlyDate" parameterType="hashmap">
		UPDATE
			TB_WPP_TAG_CODE
		SET
			TAG_DSC= #{nowDate}
		WHERE
			FUNC_TYP = #{func_typ};
	</select>
	<select id="setWppCombAndStatus" parameterType="hashmap">
		UPDATE
			TB_WPP_TAG_CODE
		SET
			DEFAULT_VALUE= #{value},
			TAG = #{pumpComb}
		WHERE
			FUNC_TYP = #{func_typ};
	</select>
	
	<select id="getPumpCombination" resultType="hashmap" parameterType="hashmap">
		SELECT
			PUMP_IDX, PUMP_TYP
		FROM
			TB_PUMP_COMBINATION
		WHERE
			PUMP_GRP = #{pump_grp}
			AND
			PUMP_COUNT = #{pump_count}
			AND
			PUMP_PRIORITY = #{pump_priority}
			AND
			PUMP_YN = 1
		ORDER BY
			PUMP_IDX;
	</select>
	
	<select id="getCalSameCountComb" resultType="hashmap" parameterType="hashmap">
		SELECT
		PUMP_IDX, PUMP_TYP
		FROM
		TB_PUMP_COMBINATION
		WHERE
		PUMP_GRP = #{pump_grp}
		AND
		PUMP_COUNT = #{pump_count}
		AND
		PUMP_YN = 1
		ORDER BY
		PUMP_PRIORITY, PUMP_IDX;
	</select>
	
	<select id="getGosanOptLevel" resultType="Double" parameterType="int">
		SELECT CAST(DEFAULT_VALUE AS DOUBLE) AS DEFAULT_VALUE
		FROM TB_WPP_TAG_CODE
		WHERE
		FUNC_TYP = 'NGHTM_CNDTN'
		AND TAG_DSC = #{hour};
	</select>
	
	<select id="getHujaOptLevel" resultType="Double" parameterType="int">
		SELECT CAST(DEFAULT_VALUE AS DOUBLE) AS DEFAULT_VALUE
		FROM TB_WPP_TAG_CODE
		WHERE
		FUNC_TYP = 'HUJA_CNDTN'
		AND TAG_DSC = #{hour};
	</select>
	
	<select id="getGrpFlPreTag" parameterType="int" resultType="hashmap">
		SELECT
		PUMP_GRP,
		FRI_TAG,
		PRI_T_TAG
		FROM
		TB_CTR_PRF_PUMPMST_INF
		<if test="pump_grp != 0">
			WHERE
			PUMP_GRP = #{pump_grp}
		</if>
		GROUP BY PUMP_GRP;
	</select>
	
	<insert id="savePumpComb" parameterType="hashMap">
		UPDATE
		TB_PUMP_COMBINATION
		SET
		PUMP_YN= #{PUMP_YN}
		WHERE
		PUMP_GRP= #{PUMP_GRP}
		AND
		PUMP_IDX = #{PUMP_IDX}
		AND
		PUMP_COUNT = #{PUMP_COUNT}
		AND
		PUMP_PRIORITY = #{PUMP_PRIORITY};
	</insert>

	<update id="updatePumpComb">
		UPDATE
		TB_PUMP_CAL
		SET PUMP_COMB = #{pumpComb}
		WHERE COUNT_IDX = #{countIdx}
	</update>
	
	<select id="selectPumpCombList" resultType="hashmap" parameterType="hashmap">
		SELECT
		PUMP_GRP,
		COMB_NAME,
		PUMP_COUNT,
		PUMP_PRIORITY,
		PUMP_PRIORITY_DCS,
		PUMP_RULES,
		USE_YN
		FROM
		TB_PUMP_COMBINATION_INF
		<if test="pump_grp != null and pump_grp != '' and pump_grp != '0'">
			WHERE
			PUMP_GRP = #{pump_grp}
		</if>
		ORDER BY PUMP_COUNT, PUMP_PRIORITY
	</select>
	
	<update id="updatePumpCombItem" parameterType="hashmap">
		UPDATE
		TB_PUMP_COMBINATION_INF
		SET
		USE_YN = #{use_yn}
		WHERE
		PUMP_GRP = #{use_yn}
		AND PUMP_COUNT = #{use_yn}
		AND PUMP_PRIORITY = #{pump_priority}
	</update>
	
	<select id="getPumpCombinationItem" parameterType="hashmap" resultType="hashmap">
		SELECT
		tpc.PUMP_GRP,
		tpc.PUMP_IDX,
		tpc.PUMP_COUNT,
		tpc.PUMP_PRIORITY,
		tcppi.PUMP_GRP_NM,
		tcppi.PUMP_GRP_IDX,
		tpc.PUMP_YN,
		tcppi.PUMP_NM,
		tpc.PUMP_SIZE,
		tpc.PUMP_TYP
		FROM
		TB_PUMP_COMBINATION tpc
		LEFT JOIN
		TB_CTR_PRF_PUMPMST_INF tcppi
		ON tpc.PUMP_IDX = tcppi.PUMP_IDX
		WHERE
		tpc.PUMP_GRP = #{pump_grp}
		AND
		tpc.PUMP_COUNT = #{pump_count}
		AND
		tpc.PUMP_PRIORITY = #{pump_priority}
		ORDER BY PUMP_IDX;
	</select>
	
	<select id="getGroupPumpCal" resultType="hashmap" parameterType="hashmap">
		SELECT
		*
		FROM
		TB_PUMP_CAL
		WHERE
		PUMP_GRP = #{pump_grp}
		AND
		PUMP_COUNT = #{pump_count}
		AND
		PUMP_PRIORITY = #{pump_priority}
		ORDER BY
		C_IDX, PUMP_COUNT, PUMP_PRIORITY, COUNT_IDX;
	</select>
	<update id="setPumpListYn" parameterType="hashmap">
		UPDATE TB_PUMP_COMBINATION_INF
		SET USE_YN = #{USE_YN}
		WHERE
		PUMP_GRP = #{PUMP_GRP}
		AND
		PUMP_COUNT = #{PUMP_COUNT}
		AND
		PUMP_PRIORITY = #{PUMP_PRIORITY}
	</update>

	<update id="setPumpUseYn" parameterType="hashmap">
		UPDATE TB_PUMP_CAL
		SET USE_YN = #{USE_YN}
		WHERE
		PUMP_COUNT = #{PUMP_COUNT}
	</update>

	<update id="disableGroupPumpCal" parameterType="hashmap">
		UPDATE TB_PUMP_CAL
		SET USE_YN = 0
		WHERE
		PUMP_GRP = #{PUMP_GRP}
		AND
		C_IDX = #{C_IDX}
		AND
		PUMP_COUNT = #{PUMP_COUNT}
		AND
		PUMP_PRIORITY = #{PUMP_PRIORITY};
	</update>
	<update id="enableGroupPumpCal" parameterType="hashmap">
		UPDATE TB_PUMP_CAL
		SET USE_YN = 1
		WHERE
		PUMP_GRP = #{PUMP_GRP}
		AND
		C_IDX = #{C_IDX}
		AND
		PUMP_COUNT = #{PUMP_COUNT}
		AND
		PUMP_PRIORITY = #{PUMP_PRIORITY}
	</update>
	<select id="getGrpPumpComb" parameterType="int" resultType="hashmap">
		SELECT PUMP_COMB, P_ADD_VAL, P_MUL_VAL, P_SQRT_MUL_VAL, FC_MIN_VAL, FC_MAX_VAL
		FROM TB_PUMP_CAL
		WHERE
		PUMP_GRP = #{grp}
		AND
		C_ORD = 1
		ORDER BY
		PUMP_COUNT, PUMP_PRIORITY, COUNT_IDX, C_ORD ;
	</select>
	<insert id="insertCombPwrUnit" parameterType="hashmap">
		INSERT INTO EMS_DB.TB_PUMP_COMB_POWER
		(PUMP_COMB, PWR_UNIT_COST, ESTM_PWR, AVG_FLOW)
		VALUES
		<foreach collection="list" item="mapItem" separator=",">
		(
			#{mapItem.comb},
			#{mapItem.pwrUnitCost},
			#{mapItem.pwr},
			#{mapItem.flow}
		)
		</foreach>
		ON DUPLICATE KEY UPDATE
		PWR_UNIT_COST = VALUES(PWR_UNIT_COST),
		ESTM_PWR = VALUES(ESTM_PWR),
		AVG_FLOW = VALUES(AVG_FLOW)
	</insert>
	<select id="selectWaterLevel" resultType="HashMap">
		SELECT TS, TAGNAME, VALUE
		FROM TB_RAWDATA
		WHERE TAGNAME IN (
		'ZG01Q0_P_WTDWLM011_ILE',
		'WB01Q0_P_WTDWLM021_ILE',
		'WB02Q0_P_WTDWLM021_ILE',
		'WJ55Q0_P_WTDWLE011_ILE',
		'WS03Q0_P_WTDWLM011_ILE'
		)
		AND TS >= DATE_SUB(NOW(), INTERVAL 6 HOUR);
	</select>

	<!--
	  수위 5개 태그를 정각 시점 리스트(tsList)로 조회.
	  전력원단위 차트와 동일 시간 윈도우의 9개 정각 중 과거/현재 5개(t-6h, t-3h, t-2h, t-1h, t) 사용.
	-->
	<select id="selectWaterLevelByRange" parameterType="hashmap" resultType="HashMap">
		SELECT TS, TAGNAME, VALUE
		FROM TB_RAWDATA
		WHERE TAGNAME IN (
		'ZG01Q0_P_WTDWLM011_ILE',
		'WB01Q0_P_WTDWLM021_ILE',
		'WB02Q0_P_WTDWLM021_ILE',
		'WJ55Q0_P_WTDWLE011_ILE',
		'WS03Q0_P_WTDWLM011_ILE'
		)
		AND TS IN
		<foreach item="ts" collection="tsList" open="(" separator="," close=")">
			#{ts}
		</foreach>
		ORDER BY TS, TAGNAME
	</select>

	<!--
	  수위 태그를 1분 단위 BETWEEN 범위로 조회.
	  호출부에서 tagList(List&lt;String&gt;) 로 조회 대상 태그를 동적 전달.
	  배수지별 수조 다중 태그(예: 공릉 6지, 월계 4지 등) 모두 조회되어
	  ${배수지}_AVG / _AVG_W1 / _AVG_W2 평균 산출에 반영된다.
	-->
	<select id="selectWaterLevelByMinuteRange" parameterType="hashmap" resultType="HashMap">
		SELECT TS, TAGNAME, VALUE
		FROM TB_RAWDATA
		WHERE TAGNAME IN
		<foreach collection="tagList" item="tag" open="(" separator="," close=")">
			#{tag}
		</foreach>
		AND TS &gt;= #{startDate}
		AND TS &lt;= #{endDate}
		AND SECOND(TS) = 0
		ORDER BY TS, TAGNAME
	</select>

	<!--
	  PMB 는 불규칙 시각에 기록되므로, 각 펌프(TAGNAME)별 10분 버킷마다 그 구간의
	  "마지막 TS 값" 1건만 뽑아 반환한다(차트는 10분 단위로 표출).
	  버킷 키 = FLOOR(UNIX_TIMESTAMP(TS)/600) (10분=600초). 행 수 = 펌프수 × 버킷수 로 축소.
	-->
	<select id="selectPumpRunHistoryByGrp" parameterType="hashmap" resultType="HashMap">
		SELECT t.TS AS TS, t.TAGNAME AS TAGNAME, t.VALUE AS VALUE, t.PUMP_IDX AS PUMP_IDX
		FROM (
			SELECT
				rd.TS         AS TS,
				rd.TAGNAME    AS TAGNAME,
				rd.VALUE      AS VALUE,
				cppi.PUMP_IDX AS PUMP_IDX,
				ROW_NUMBER() OVER (
					PARTITION BY rd.TAGNAME, FLOOR(UNIX_TIMESTAMP(rd.TS) / 600)
					ORDER BY rd.TS DESC
				) AS rn
			FROM TB_RAWDATA rd
			JOIN TB_CTR_PRF_PUMPMST_INF cppi
			  ON cppi.PMB_TAG = rd.TAGNAME
			WHERE cppi.PUMP_GRP = #{PUMP_GRP}
			  AND cppi.USE_YN = '1'
			  AND rd.TS &gt;= #{startDate}
			  AND rd.TS &lt;= #{endDate}
		) t
		WHERE t.rn = 1
		ORDER BY t.TS
	</select>

	<!--
	  윈도 시작 직전 각 펌프의 마지막 ON/OFF 상태 시드 조회.
	  - selectPumpRunHistoryByGrp 의 startDate 직전 시점에 각 PMB_TAG 별 최신 VALUE 1건씩 반환.
	  - PMB 가 변경 시점 위주로 기록될 때 daysAgo>0 윈도 초입이 OFF 로 잘못 그려지는 문제 방지.
	  - seedStartDate: 조회 하한. startDate 이전 "전체 이력" 풀스캔을 막기 위한 lower bound(서비스에서 startDate - SEED_LOOKBACK 로 산출).
	-->
	<select id="selectPumpLastStateBeforeGrp" parameterType="hashmap" resultType="HashMap">
		SELECT
			rd.TAGNAME    AS TAGNAME,
			rd.VALUE      AS VALUE,
			cppi.PUMP_IDX AS PUMP_IDX
		FROM TB_RAWDATA rd
		JOIN TB_CTR_PRF_PUMPMST_INF cppi
		  ON cppi.PMB_TAG = rd.TAGNAME
		JOIN (
			SELECT rd2.TAGNAME AS TAGNAME, MAX(rd2.TS) AS MAX_TS
			FROM TB_RAWDATA rd2
			JOIN TB_CTR_PRF_PUMPMST_INF cppi2
			  ON cppi2.PMB_TAG = rd2.TAGNAME
			WHERE cppi2.PUMP_GRP = #{PUMP_GRP}
			  AND cppi2.USE_YN = '1'
			  AND rd2.TS &lt; #{startDate}
			  AND rd2.TS &gt;= #{seedStartDate}
			GROUP BY rd2.TAGNAME
		) latest
		  ON latest.TAGNAME = rd.TAGNAME
		 AND latest.MAX_TS  = rd.TS
		WHERE cppi.PUMP_GRP = #{PUMP_GRP}
		  AND cppi.USE_YN = '1'
	</select>

	<!--
	  10분 단위 IWH/UFR 실측 조회 (TB_RAWDATA).
	  - tagList: 조회할 태그명 리스트 (전력 9개 + 유량 1개 등)
	  - startDate, endDate: TS 범위 (양 끝 포함)
	  - 10분 정렬된 분 단위 값만 반환.
	-->
	<select id="selectMultiTagRawRange" parameterType="hashmap" resultType="hashmap">
		SELECT
			TAGNAME as tag,
			DATE_FORMAT(TS, '%Y-%m-%d %H:%i') as ts,
			VALUE as value
		FROM TB_RAWDATA
		WHERE TAGNAME IN
			<foreach item="tag" collection="tagList" open="(" separator="," close=")">
				#{tag}
			</foreach>
			AND TS &gt;= #{startDate}
			AND TS &lt;= #{endDate}
			AND MINUTE(TS) % 10 = 0
			AND SECOND(TS) = 0
		ORDER BY TS, TAGNAME
	</select>

	<!--
	  1분 단위 IWH/UFR 실측 조회 (TB_RAWDATA).
	  selectMultiTagRawRange 와 달리 MINUTE % 10 / SECOND=0 필터 없이 ts 를 분 단위로 그룹화.
	  → 적재 SECOND 가 0이 아닌 경우(예: 14:38:23)도 14:38 키로 정상 집계.
	  같은 (TAG, 분) 에 여러 row 가 있으면 가장 늦은 초의 값을 채택.
	-->
	<select id="selectMultiTagRawRangeMinutely" parameterType="hashmap" resultType="hashmap">
		SELECT
			tag,
			ts,
			value
		FROM (
			SELECT
				TAGNAME as tag,
				DATE_FORMAT(TS, '%Y-%m-%d %H:%i') as ts,
				VALUE as value,
				ROW_NUMBER() OVER (
					PARTITION BY TAGNAME, DATE_FORMAT(TS, '%Y-%m-%d %H:%i')
					ORDER BY TS DESC
				) as rn
			FROM TB_RAWDATA
			WHERE TAGNAME IN
				<foreach item="tag" collection="tagList" open="(" separator="," close=")">
					#{tag}
				</foreach>
				AND TS &gt;= #{startDate}
				AND TS &lt;= #{endDate}
		) t
		WHERE rn = 1
		ORDER BY ts, tag
	</select>

	<!--
	  10분 단위 펌프 가동 여부 실측 조회 (TB_RAWDATA + PMB_TAG).
	  - pumpGrp: 펌프 계통
	  - startDate, endDate: TS 범위
	  반환: [{ts, pumpIdx, pumpYn(0/1)}]
	-->
	<select id="selectMultiTimePumpYnRaw" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts,
			tcppi.PUMP_IDX as pumpIdx,
			tr.VALUE as pumpYn
		FROM TB_CTR_PRF_PUMPMST_INF tcppi
		JOIN TB_RAWDATA tr ON tr.TAGNAME = tcppi.PMB_TAG
		WHERE tcppi.PUMP_GRP = #{pumpGrp}
			AND tr.TS &gt;= #{startDate}
			AND tr.TS &lt;= #{endDate}
			AND MINUTE(tr.TS) % 10 = 0
			AND SECOND(tr.TS) = 0
		ORDER BY tr.TS, tcppi.PUMP_IDX
	</select>

	<!--
	  1분 단위 펌프 가동 여부 실측 조회 (TB_RAWDATA + PMB_TAG).
	  selectMultiTimePumpYnRaw 와 달리 MINUTE % 10 / SECOND=0 필터 없이 분 단위 그룹화.
	  같은 (PUMP_IDX, 분) 에 여러 row 가 있으면 가장 늦은 초의 값을 채택.
	-->
	<select id="selectMultiTimePumpYnRawMinutely" parameterType="hashmap" resultType="hashmap">
		SELECT
			ts,
			pumpIdx,
			pumpYn
		FROM (
			SELECT
				DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') as ts,
				tcppi.PUMP_IDX as pumpIdx,
				tr.VALUE as pumpYn,
				ROW_NUMBER() OVER (
					PARTITION BY tcppi.PUMP_IDX, DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i')
					ORDER BY tr.TS DESC
				) as rn
			FROM TB_CTR_PRF_PUMPMST_INF tcppi
			JOIN TB_RAWDATA tr ON tr.TAGNAME = tcppi.PMB_TAG
			WHERE tcppi.PUMP_GRP = #{pumpGrp}
				AND tr.TS &gt;= #{startDate}
				AND tr.TS &lt;= #{endDate}
		) t
		WHERE rn = 1
		ORDER BY ts, pumpIdx
	</select>

	<!--
	  가장 최근 RGSTR_TIME 기준 펌프별 예측 가동 여부 조회.
	  TB_CTR_PUMPYN_RST 에 PRDCT_TIME 컬럼이 없어 시점별 분리는 불가 — 가장 최근 RGSTR_TIME 의 가동 조합을 미래 시점 전체에 적용.
	  반환: [{pumpIdx, pumpYn}]
	-->
	<select id="selectLatestPumpYnPrdct" parameterType="hashmap" resultType="hashmap">
		SELECT
			PUMP_IDX as pumpIdx,
			PUMP_YN as pumpYn
		FROM TB_CTR_PUMPYN_RST
		WHERE PUMP_GRP = #{pumpGrp}
			AND RGSTR_TIME = (
				SELECT MAX(RGSTR_TIME) FROM TB_CTR_PUMPYN_RST
				WHERE PUMP_GRP = #{pumpGrp}
					AND RGSTR_TIME &lt;= #{nowDateTime}
			)
	</select>

	<!--
	  10분 단위 IWH/UFR 예측 조회 (TB_CTR_TNK_RST).
	  - tagList: DSTRB_ID 리스트
	  - prdctTimeList: 미래 PRDCT_TIME 리스트 (예: ["2026-05-06 13:50","2026-05-06 14:00",...])
	  - nowDateTime: 현재 시각. 이 시각 이하의 RGSTR_TIME 중 가장 최근 행을 사용.
	  태그×시점 조합별로 가장 최근 RGSTR_TIME 의 PRDCT_VALUE 1건을 반환.
	-->
	<select id="selectMultiTagPrdctRange" parameterType="hashmap" resultType="hashmap">
		SELECT
			t.DSTRB_ID as tag,
			DATE_FORMAT(t.PRDCT_TIME, '%Y-%m-%d %H:%i') as ts,
			t.PRDCT_VALUE as value
		FROM TB_CTR_TNK_RST t
		JOIN (
			SELECT DSTRB_ID, PRDCT_TIME, MAX(RGSTR_TIME) as MAX_RGSTR
			FROM TB_CTR_TNK_RST
			WHERE DSTRB_ID IN
				<foreach item="tag" collection="tagList" open="(" separator="," close=")">
					#{tag}
				</foreach>
				AND PRDCT_TIME IN
				<foreach item="ptime" collection="prdctTimeList" open="(" separator="," close=")">
					#{ptime}
				</foreach>
				AND RGSTR_TIME &lt;= #{nowDateTime}
				<if test="rgstrLowerBound != null">
				AND RGSTR_TIME &gt;= #{rgstrLowerBound}
				</if>
				AND MODEL_NAME = 'XGBoost'
			GROUP BY DSTRB_ID, PRDCT_TIME
		) m
			ON t.DSTRB_ID = m.DSTRB_ID
			AND t.PRDCT_TIME = m.PRDCT_TIME
			AND t.RGSTR_TIME = m.MAX_RGSTR
		WHERE t.MODEL_NAME = 'XGBoost'
		ORDER BY t.PRDCT_TIME, t.DSTRB_ID
	</select>

	<!--
	  과거 10분 horizon 예측 조회 (TB_CTR_TNK_RST).
	  각 PRDCT_TIME 에 대해 PRDCT_TIME = RGSTR_TIME + 10분 인 예측 행만 반환 → 실측과의 정확도 비교용.
	  - tagList: DSTRB_ID 리스트
	  - prdctTimeList: 과거 PRDCT_TIME 10분 경계 리스트
	-->
	<select id="selectMultiTagPrdct10minHorizon" parameterType="hashmap" resultType="hashmap">
		SELECT
			DSTRB_ID as tag,
			DATE_FORMAT(PRDCT_TIME, '%Y-%m-%d %H:%i') as ts,
			PRDCT_VALUE as value
		FROM TB_CTR_TNK_RST
		WHERE DSTRB_ID IN
			<foreach item="tag" collection="tagList" open="(" separator="," close=")">
				#{tag}
			</foreach>
			AND PRDCT_TIME IN
			<foreach item="ptime" collection="prdctTimeList" open="(" separator="," close=")">
				#{ptime}
			</foreach>
			AND PRDCT_TIME = DATE_ADD(RGSTR_TIME, INTERVAL 10 MINUTE)
			AND MODEL_NAME = 'XGBoost'
		ORDER BY PRDCT_TIME, DSTRB_ID
	</select>

	<!--
	  ===========================================================
	  10분 주기 예측/실측 유량·펌프조합 스냅샷 (백엔드 분석용, 화면 미노출)
	  - 적재: DrvnConfig.scheduleFlowCombSnapshotTask
	  - 테이블: TB_PUMP_FLOW_COMB_SNAPSHOT (tb_pump_flow_comb_snapshot.sql)
	  ===========================================================
	-->
	<!--
	  예측 유량: TB_CTR_TNK_RST 에서 RGSTR_TIME / PRDCT_TIME 모두 snapshot 과 정확 동치인 행의 PRDCT_VALUE.
	  대시보드 성능점/예측점 유량과 동일 행 보장. 해당 행이 아직 없으면 null 반환 → snapshot 에 null 적재.
	-->
	<select id="selectLatestPredictedFlowTnk" parameterType="hashmap" resultType="hashmap">
		SELECT
			ROUND(tctr.PRDCT_VALUE, 1) AS prdct_flow,
			DATE_FORMAT(tctr.PRDCT_TIME, '%Y-%m-%d %H:%i') AS prdct_time,
			tctr.MODEL_NAME AS model_name
		FROM TB_CTR_TNK_RST tctr
		WHERE UPPER(tctr.DSTRB_ID) = UPPER(#{dstrb_id})
			AND tctr.RGSTR_TIME = #{rgstr_time}
			AND tctr.PRDCT_TIME = #{prdct_time}
			AND tctr.MODEL_NAME = 'XGBoost'
		LIMIT 1
	</select>

	<!--
	  예측 펌프 조합: TB_CTR_PUMPYN_RST 의 최신 RGSTR_TIME (≤ rgstr_time) 에서 PUMP_YN=1 인 PUMP_IDX CSV.
	  반환: { prdct_comb, actl_ref_ts } — actl_ref_ts 는 이 예측 조합이 결정된 시각 (= MAX RGSTR_TIME).
	  실측 컬럼들(actl_flow/comb/prsr/wl) 은 이 시점의 raw 를 매치하여 "예측 조합이 참고한 시점" 과 정렬.
	-->
	<select id="selectLatestPredictedComb" parameterType="hashmap" resultType="hashmap">
		SELECT
			GROUP_CONCAT(CASE WHEN t.PUMP_YN = 1 THEN t.PUMP_IDX END ORDER BY t.PUMP_IDX SEPARATOR ',') AS prdct_comb,
			DATE_FORMAT(MAX(t.RGSTR_TIME), '%Y-%m-%d %H:%i:%s') AS actl_ref_ts
		FROM TB_CTR_PUMPYN_RST t
		WHERE t.PUMP_GRP = #{pump_grp}
			AND t.RGSTR_TIME = (
				SELECT MAX(RGSTR_TIME) FROM TB_CTR_PUMPYN_RST
				WHERE PUMP_GRP = #{pump_grp}
					AND RGSTR_TIME &lt;= #{rgstr_time}
			)
	</select>

	<!--
	  실측 유량: PUMP_GRP 의 FRI_TAG (TB_CTR_PRF_PUMPMST_INF) 로 TB_RAWDATA 의 actl_ref_ts (예측 조합이
	  결정된 시점) 와 정렬된 값. 정확 시각 raw 가 있으면 그게 잡히고, 없으면 (actl_ref_ts-10min, actl_ref_ts]
	  윈도우 내 가장 최근 raw 가 잡힘 → "예측이 본 시점" 직전 마지막 측정값.
	-->
	<select id="selectLatestActualFlow" parameterType="hashmap" resultType="hashmap">
		SELECT DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') AS ts, tr.VALUE AS value
		FROM TB_RAWDATA tr
		JOIN TB_CTR_PRF_PUMPMST_INF tcppi ON tcppi.FRI_TAG = tr.TAGNAME
		WHERE tcppi.PUMP_GRP = #{pump_grp}
			AND tr.TS &lt;= #{actl_ref_ts}
			AND tr.TS &gt; DATE_SUB(#{actl_ref_ts}, INTERVAL 10 MINUTE)
		ORDER BY tr.TS DESC
		LIMIT 1
	</select>

	<!--
	  실측 펌프 조합: actl_ref_ts (예측 조합이 결정된 시점) 기준 각 PUMP_GRP 의 PMB_TAG 최근 ON 펌프 CSV.
	-->
	<select id="selectLatestActualComb" parameterType="hashmap" resultType="String">
		SELECT GROUP_CONCAT(t.PUMP_IDX ORDER BY t.PUMP_IDX SEPARATOR ',') AS actl_comb
		FROM (
			SELECT cppi.PUMP_IDX, rd.value
			FROM TB_RAWDATA rd
			JOIN TB_CTR_PRF_PUMPMST_INF cppi ON rd.TAGNAME = cppi.PMB_TAG
			WHERE cppi.PUMP_GRP = #{pump_grp}
				AND rd.TS = (
					SELECT MAX(sub.TS) FROM TB_RAWDATA sub
					WHERE sub.TAGNAME = cppi.PMB_TAG
						AND sub.TS &lt;= #{actl_ref_ts}
						AND sub.TS &gt; DATE_SUB(#{actl_ref_ts}, INTERVAL 10 MINUTE)
				)
		) t
		WHERE t.value = 1
	</select>

	<!--
	  매분 retry 방식 UPSERT.
	  - 신규 키 (RGSTR_TIME, PUMP_GRP) 면 INSERT.
	  - 이미 존재하면 새 값이 non-null 일 때만 갱신 (COALESCE 로 기존 값 보존).
	    → 1분차에 PRDCT_FLOW 만 들어오고 다음 분차에 ACTL_FLOW 가 들어와도 둘 다 유지됨.
	-->
	<insert id="insertFlowCombSnapshot" parameterType="hashmap">
		INSERT INTO TB_PUMP_FLOW_COMB_SNAPSHOT
			(RGSTR_TIME, PRDCT_TIME, PUMP_GRP,
			 PRDCT_FLOW, ACTL_FLOW, PRDCT_COMB, ACTL_COMB,
			 PRDCT_PRSR, ACTL_PRSR,
			 PRDCT_PWR, ACTL_PWR,
			 PRDCT_WL_GN, PRDCT_WL_BA, PRDCT_WL_GR, PRDCT_WL_WG, PRDCT_WL_WGOK,
			 ACTL_WL_GN,  ACTL_WL_BA,  ACTL_WL_GR,  ACTL_WL_WG,  ACTL_WL_WGOK,
			 MODEL_NAME)
		VALUES
			(#{rgstr_time}, #{prdct_time}, #{pump_grp},
			 #{prdct_flow}, #{actl_flow}, #{prdct_comb}, #{actl_comb},
			 #{prdct_prsr}, #{actl_prsr},
			 #{prdct_pwr}, #{actl_pwr},
			 #{prdct_wl_gn}, #{prdct_wl_ba}, #{prdct_wl_gr}, #{prdct_wl_wg}, #{prdct_wl_wgok},
			 #{actl_wl_gn},  #{actl_wl_ba},  #{actl_wl_gr},  #{actl_wl_wg},  #{actl_wl_wgok},
			 #{model_name})
		ON DUPLICATE KEY UPDATE
			PRDCT_TIME    = #{prdct_time},
			PRDCT_FLOW    = COALESCE(#{prdct_flow}, PRDCT_FLOW),
			ACTL_FLOW     = COALESCE(#{actl_flow},  ACTL_FLOW),
			PRDCT_COMB    = COALESCE(#{prdct_comb}, PRDCT_COMB),
			ACTL_COMB     = COALESCE(#{actl_comb},  ACTL_COMB),
			PRDCT_PRSR    = COALESCE(#{prdct_prsr}, PRDCT_PRSR),
			ACTL_PRSR     = COALESCE(#{actl_prsr},  ACTL_PRSR),
			PRDCT_PWR     = COALESCE(#{prdct_pwr},  PRDCT_PWR),
			ACTL_PWR      = COALESCE(#{actl_pwr},   ACTL_PWR),
			PRDCT_WL_GN   = COALESCE(#{prdct_wl_gn},   PRDCT_WL_GN),
			PRDCT_WL_BA   = COALESCE(#{prdct_wl_ba},   PRDCT_WL_BA),
			PRDCT_WL_GR   = COALESCE(#{prdct_wl_gr},   PRDCT_WL_GR),
			PRDCT_WL_WG   = COALESCE(#{prdct_wl_wg},   PRDCT_WL_WG),
			PRDCT_WL_WGOK = COALESCE(#{prdct_wl_wgok}, PRDCT_WL_WGOK),
			ACTL_WL_GN    = COALESCE(#{actl_wl_gn},    ACTL_WL_GN),
			ACTL_WL_BA    = COALESCE(#{actl_wl_ba},    ACTL_WL_BA),
			ACTL_WL_GR    = COALESCE(#{actl_wl_gr},    ACTL_WL_GR),
			ACTL_WL_WG    = COALESCE(#{actl_wl_wg},    ACTL_WL_WG),
			ACTL_WL_WGOK  = COALESCE(#{actl_wl_wgok},  ACTL_WL_WGOK),
			MODEL_NAME    = COALESCE(#{model_name},    MODEL_NAME)
	</insert>

	<!--
	  (정확도) 항목별 오차율/정확도 UPSERT. 스냅샷과 동일 PK (RGSTR_TIME, PUMP_GRP).
	  - 재계산 시 항상 최신 계산값으로 덮어씀(COALESCE 미사용): ACTL 이 늦게 채워지면 다음 주기에 갱신.
	-->
	<insert id="insertFlowCombAccuracy" parameterType="hashmap">
		INSERT INTO TB_PUMP_FLOW_COMB_ACCURACY
			(RGSTR_TIME, PRDCT_TIME, PUMP_GRP,
			 ERR_FLOW, ACC_FLOW, ERR_PRSR, ACC_PRSR,
			 ERR_WL_GN, ACC_WL_GN, ERR_WL_BA, ACC_WL_BA, ERR_WL_GR, ACC_WL_GR,
			 ERR_WL_WG, ACC_WL_WG, ERR_WL_WGOK, ACC_WL_WGOK,
			 ERR_COMB, ACC_COMB, CALC_TIME)
		VALUES
			(#{rgstr_time}, #{prdct_time}, #{pump_grp},
			 #{err_flow}, #{acc_flow}, #{err_prsr}, #{acc_prsr},
			 #{err_wl_gn}, #{acc_wl_gn}, #{err_wl_ba}, #{acc_wl_ba}, #{err_wl_gr}, #{acc_wl_gr},
			 #{err_wl_wg}, #{acc_wl_wg}, #{err_wl_wgok}, #{acc_wl_wgok},
			 #{err_comb}, #{acc_comb}, NOW())
		ON DUPLICATE KEY UPDATE
			PRDCT_TIME  = #{prdct_time},
			ERR_FLOW    = #{err_flow},    ACC_FLOW    = #{acc_flow},
			ERR_PRSR    = #{err_prsr},    ACC_PRSR    = #{acc_prsr},
			ERR_WL_GN   = #{err_wl_gn},   ACC_WL_GN   = #{acc_wl_gn},
			ERR_WL_BA   = #{err_wl_ba},   ACC_WL_BA   = #{acc_wl_ba},
			ERR_WL_GR   = #{err_wl_gr},   ACC_WL_GR   = #{acc_wl_gr},
			ERR_WL_WG   = #{err_wl_wg},   ACC_WL_WG   = #{acc_wl_wg},
			ERR_WL_WGOK = #{err_wl_wgok}, ACC_WL_WGOK = #{acc_wl_wgok},
			ERR_COMB    = #{err_comb},    ACC_COMB    = #{acc_comb},
			CALC_TIME   = NOW()
	</insert>

	<!--
	  (엑셀 다운로드) 기간 [from, to] 스냅샷 + 정확도 결합 행.
	  - SNAPSHOT 을 기준으로 LEFT JOIN ACCURACY (PK: RGSTR_TIME, PUMP_GRP 동일).
	  - 수위(WL_*) 는 SQL 단계에서 소수 둘째 자리 ROUND.
	  - 다른 숫자(유량/압력) 는 DB 원본 유지. 정확도/오차율은 저장 시 이미 round2.
	-->
	<select id="selectSnapshotsWithAccuracyByRange" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(s.RGSTR_TIME, '%Y-%m-%d %H:%i:%s') AS rgstr_time,
			DATE_FORMAT(s.PRDCT_TIME, '%Y-%m-%d %H:%i:%s') AS prdct_time,
			s.PUMP_GRP                  AS pump_grp,

			s.PRDCT_FLOW                AS prdct_flow,
			s.ACTL_FLOW                 AS actl_flow,
			a.ACC_FLOW                  AS acc_flow,
			a.ERR_FLOW                  AS err_flow,

			s.PRDCT_PRSR                AS prdct_prsr,
			s.ACTL_PRSR                 AS actl_prsr,
			a.ACC_PRSR                  AS acc_prsr,
			a.ERR_PRSR                  AS err_prsr,

			ROUND(s.PRDCT_WL_GN, 2)     AS prdct_wl_gn,
			ROUND(s.ACTL_WL_GN, 2)      AS actl_wl_gn,
			a.ACC_WL_GN                 AS acc_wl_gn,
			a.ERR_WL_GN                 AS err_wl_gn,

			ROUND(s.PRDCT_WL_BA, 2)     AS prdct_wl_ba,
			ROUND(s.ACTL_WL_BA, 2)      AS actl_wl_ba,
			a.ACC_WL_BA                 AS acc_wl_ba,
			a.ERR_WL_BA                 AS err_wl_ba,

			ROUND(s.PRDCT_WL_GR, 2)     AS prdct_wl_gr,
			ROUND(s.ACTL_WL_GR, 2)      AS actl_wl_gr,
			a.ACC_WL_GR                 AS acc_wl_gr,
			a.ERR_WL_GR                 AS err_wl_gr,

			ROUND(s.PRDCT_WL_WG, 2)     AS prdct_wl_wg,
			ROUND(s.ACTL_WL_WG, 2)      AS actl_wl_wg,
			a.ACC_WL_WG                 AS acc_wl_wg,
			a.ERR_WL_WG                 AS err_wl_wg,

			ROUND(s.PRDCT_WL_WGOK, 2)   AS prdct_wl_wgok,
			ROUND(s.ACTL_WL_WGOK, 2)    AS actl_wl_wgok,
			a.ACC_WL_WGOK               AS acc_wl_wgok,
			a.ERR_WL_WGOK               AS err_wl_wgok,

			s.PRDCT_COMB                AS prdct_comb,
			s.ACTL_COMB                 AS actl_comb,
			a.ACC_COMB                  AS acc_comb,
			a.ERR_COMB                  AS err_comb,

			DATE_FORMAT(a.CALC_TIME, '%Y-%m-%d %H:%i:%s') AS calc_time
		FROM TB_PUMP_FLOW_COMB_SNAPSHOT s
		LEFT JOIN TB_PUMP_FLOW_COMB_ACCURACY a
			   ON a.RGSTR_TIME = s.RGSTR_TIME AND a.PUMP_GRP = s.PUMP_GRP
		WHERE s.RGSTR_TIME &gt;= #{from}
			AND s.RGSTR_TIME &lt;= #{to}
		ORDER BY s.RGSTR_TIME DESC, s.PUMP_GRP ASC
	</select>

	<!--
	  예측 압력: TB_CTR_TNK_RST 에서 압력 DSTRB_ID(pumpDstrbIdMap[grp].pressure) 의
	  RGSTR_TIME / PRDCT_TIME 정확 동치 행 PRDCT_VALUE. 예측 유량과 동일 패턴.
	-->
	<select id="selectLatestPredictedPressureTnk" parameterType="hashmap" resultType="hashmap">
		SELECT
			ROUND(CAST(tctr.PRDCT_VALUE AS DOUBLE), 2) AS prdct_prsr,
			DATE_FORMAT(tctr.PRDCT_TIME, '%Y-%m-%d %H:%i') AS prdct_time
		FROM TB_CTR_TNK_RST tctr
		WHERE UPPER(tctr.DSTRB_ID) = UPPER(#{dstrb_id})
			AND tctr.RGSTR_TIME = #{rgstr_time}
			AND tctr.PRDCT_TIME = #{prdct_time}
			AND tctr.MODEL_NAME = 'XGBoost'
		LIMIT 1
	</select>

	<!--
	  실측 압력: 동일 압력 태그(TB_RAWDATA.TAGNAME = pumpDstrbIdMap[grp].pressure) 의
	  (actl_ref_ts-10min, actl_ref_ts] 윈도우 가장 최근 행. selectLatestActualFlow 와 동일 매치 정책.
	-->
	<select id="selectLatestActualPressure" parameterType="hashmap" resultType="hashmap">
		SELECT DATE_FORMAT(tr.TS, '%Y-%m-%d %H:%i') AS ts, tr.VALUE AS value
		FROM TB_RAWDATA tr
		WHERE tr.TAGNAME = #{tag}
			AND tr.TS &lt;= #{actl_ref_ts}
			AND tr.TS &gt; DATE_SUB(#{actl_ref_ts}, INTERVAL 10 MINUTE)
		ORDER BY tr.TS DESC
		LIMIT 1
	</select>

	<!--
	  예측 배수지 수위 평균: TB_CTR_TNK_RST 에서 배수지의 수조 태그(tagList) 중
	  PRDCT_VALUE >= threshold 인 active 수조만 골라 평균 (frontend _AVG 와 동일 규칙).
	  threshold: 구리 1.5m, 나머지 3.0m (drvnConfig.getSeoulReservoirThreshold).
	-->
	<select id="selectLatestPredictedReservoirAvg" parameterType="hashmap" resultType="hashmap">
		SELECT AVG(tctr.PRDCT_VALUE) AS avg_value, COUNT(*) AS tank_count
		FROM TB_CTR_TNK_RST tctr
		WHERE tctr.DSTRB_ID IN
			<foreach collection="tagList" item="tag" open="(" separator="," close=")">
				#{tag}
			</foreach>
			AND tctr.RGSTR_TIME = #{rgstr_time}
			AND tctr.PRDCT_TIME = #{prdct_time}
			AND tctr.MODEL_NAME = 'XGBoost'
			AND tctr.PRDCT_VALUE IS NOT NULL
			AND tctr.PRDCT_VALUE <![CDATA[ >= ]]> #{threshold}
	</select>

	<!--
	  실측 배수지 수위 평균: TB_RAWDATA 에서 배수지의 수조 태그(tagList) 중 (actl_ref_ts-10min, actl_ref_ts]
	  윈도우 내 각 태그별 가장 최근 행을 골라 active(>=threshold) 수조만 평균.
	  태그별 1개 행만 사용 → 한 태그가 윈도우 내 여러 raw 있어도 평균에 가중치 없음.
	-->
	<select id="selectLatestActualReservoirAvg" parameterType="hashmap" resultType="hashmap">
		SELECT AVG(latest.VALUE) AS avg_value, COUNT(*) AS tank_count
		FROM (
			SELECT tr.TAGNAME, tr.VALUE
			FROM TB_RAWDATA tr
			JOIN (
				SELECT TAGNAME, MAX(TS) AS last_ts
				FROM TB_RAWDATA
				WHERE TAGNAME IN
					<foreach collection="tagList" item="tag" open="(" separator="," close=")">
						#{tag}
					</foreach>
					AND TS &lt;= #{actl_ref_ts}
					AND TS &gt; DATE_SUB(#{actl_ref_ts}, INTERVAL 10 MINUTE)
					AND VALUE IS NOT NULL
				GROUP BY TAGNAME
			) m ON m.TAGNAME = tr.TAGNAME AND m.last_ts = tr.TS
			WHERE tr.VALUE <![CDATA[ >= ]]> #{threshold}
		) latest
	</select>

	<!--
	  [snapshot backfill] 최근 N 시간 내 한 컬럼이라도 null 인 snapshot 행 조회.
	  retry 대상: PRDCT_FLOW, ACTL_FLOW, PRDCT_PRSR, ACTL_PRSR,
	             PRDCT_WL_{GN,BA,GR,WG,WGOK}, ACTL_WL_{GN,BA,GR,WG,WGOK}.
	  PRDCT_COMB / ACTL_COMB 는 본 backfill 대상 아님 (별도 로직).
	  주의: pressure 컬럼은 해당 pump_grp 가 pressure 미설정이면 영구 null 이라
	         같은 행이 매분 반환됨 → backfill 측에서 dstrbId 없으면 skip 처리.
	-->
	<select id="selectRecentSnapshotsWithNulls" parameterType="hashmap" resultType="hashmap">
		SELECT
			DATE_FORMAT(RGSTR_TIME, '%Y-%m-%d %H:%i:%s') AS rgstr_time,
			DATE_FORMAT(PRDCT_TIME, '%Y-%m-%d %H:%i:%s') AS prdct_time,
			PUMP_GRP      AS pump_grp,
			PRDCT_FLOW    AS prdct_flow,
			ACTL_FLOW     AS actl_flow,
			PRDCT_COMB    AS prdct_comb,
			ACTL_COMB     AS actl_comb,
			PRDCT_PRSR    AS prdct_prsr,
			ACTL_PRSR     AS actl_prsr,
			PRDCT_PWR     AS prdct_pwr,
			ACTL_PWR      AS actl_pwr,
			PRDCT_WL_GN   AS prdct_wl_gn,
			PRDCT_WL_BA   AS prdct_wl_ba,
			PRDCT_WL_GR   AS prdct_wl_gr,
			PRDCT_WL_WG   AS prdct_wl_wg,
			PRDCT_WL_WGOK AS prdct_wl_wgok,
			ACTL_WL_GN    AS actl_wl_gn,
			ACTL_WL_BA    AS actl_wl_ba,
			ACTL_WL_GR    AS actl_wl_gr,
			ACTL_WL_WG    AS actl_wl_wg,
			ACTL_WL_WGOK  AS actl_wl_wgok
		FROM TB_PUMP_FLOW_COMB_SNAPSHOT
		WHERE RGSTR_TIME &gt;= DATE_SUB(#{nowDateTime}, INTERVAL #{hoursBack} HOUR)
			AND RGSTR_TIME &lt; #{nowDateTime}
			AND (
				PRDCT_FLOW    IS NULL OR ACTL_FLOW    IS NULL OR
				PRDCT_PRSR    IS NULL OR ACTL_PRSR    IS NULL OR
				PRDCT_PWR     IS NULL OR ACTL_PWR     IS NULL OR
				PRDCT_WL_GN   IS NULL OR PRDCT_WL_BA  IS NULL OR PRDCT_WL_GR IS NULL OR
				PRDCT_WL_WG   IS NULL OR PRDCT_WL_WGOK IS NULL OR
				ACTL_WL_GN    IS NULL OR ACTL_WL_BA   IS NULL OR ACTL_WL_GR  IS NULL OR
				ACTL_WL_WG    IS NULL OR ACTL_WL_WGOK IS NULL
			)
		ORDER BY RGSTR_TIME ASC
	</select>

	<!--
	  (10분 스냅샷) 실측 전력: 설정(dstrb.prdct.pumpActivePwrTag) 의 유효전력 IKW 태그(9개) 를
	  TB_RAWDATA 에서 actl_ref_ts (예측 조합이 결정된 시점)
	  윈도우 (actl_ref_ts-10min, actl_ref_ts] 태그별 최신 1건만 골라 단순 SUM.
	  IKW 는 순시 kW 이므로 차분/단위환산 없이 그대로 합산. 시스템 전체값(PUMP_GRP 무관).
	-->
	<select id="selectLatestActualPowerSum" parameterType="hashmap" resultType="hashmap">
		SELECT ROUND(SUM(latest.VALUE), 2) AS value, COUNT(*) AS tag_count
		FROM (
			SELECT tr.TAGNAME, tr.VALUE
			FROM TB_RAWDATA tr
			JOIN (
				SELECT rd.TAGNAME, MAX(rd.TS) AS last_ts
				FROM TB_RAWDATA rd
				WHERE rd.TAGNAME IN
					<foreach collection="pwrTagList" item="tag" open="(" separator="," close=")">
						#{tag}
					</foreach>
					AND rd.TS &lt;= #{actl_ref_ts}
					AND rd.TS &gt; DATE_SUB(#{actl_ref_ts}, INTERVAL 10 MINUTE)
					AND rd.VALUE IS NOT NULL
				GROUP BY rd.TAGNAME
			) m ON m.TAGNAME = tr.TAGNAME AND m.last_ts = tr.TS
		) latest
	</select>

	<!--
	  (10분 스냅샷) 예측 전력: TB_PEAK_PWR_PRDCT_RST.PRDCT_PWR.
	  매칭(사용자 확정): CNFRM_TIME 을 10분 경계로 내림 = snapshot.RGSTR_TIME,
	                    ANLY_TIME  을 10분 경계로 내림 = snapshot.PRDCT_TIME.
	    → CNFRM_TIME ∈ [rgstr_time, prdct_time)        (prdct_time = rgstr_time + 10분)
	      ANLY_TIME  ∈ [prdct_time, prdct_time + 10분)
	  동일 경계 내 여러 건이면 최신(CNFRM_TIME, ANLY_TIME) 1건. 시스템 전체값(PUMP_GRP 무관).
	-->
	<select id="selectLatestPredictedPower" parameterType="hashmap" resultType="hashmap">
		SELECT
			PRDCT_PWR AS prdct_pwr,
			DATE_FORMAT(CNFRM_TIME, '%Y-%m-%d %H:%i:%s') AS cnfrm_time,
			DATE_FORMAT(ANLY_TIME,  '%Y-%m-%d %H:%i:%s') AS anly_time
		FROM TB_PEAK_PWR_PRDCT_RST
		WHERE CNFRM_TIME &gt;= #{rgstr_time}
			AND CNFRM_TIME &lt; #{prdct_time}
			AND ANLY_TIME  &gt;= #{prdct_time}
			AND ANLY_TIME  &lt; DATE_ADD(#{prdct_time}, INTERVAL 10 MINUTE)
		ORDER BY CNFRM_TIME DESC, ANLY_TIME DESC
		LIMIT 1
	</select>

</mapper>
